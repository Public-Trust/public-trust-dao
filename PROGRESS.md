# PROGRESS — Public Trust DAO

Журнал прогресса автономного строителя. Обновляется каждой сессией. Источник
истины о состоянии — `git log`, этот файл — карта «что дальше».

## Состояние

- **2026-06-22 (bootstrap-сессия):** собран автономный контур (пульс).
  - `BUILDER.md` — мандат агента-строителя.
  - `scripts/loop.sh` — петля claude -p (пульс), измеряет коммиты по git.
  - `scripts/report.sh` — Telegram-репортер.
  - systemd-сервис `public-trust-builder` — запускает пульс, Restart=always.
  - `PROGRESS.md` / `DECISIONS.md` — память.
  - Скелет репо: `/docs` (MANIFESTO.md), `/contracts`, `/web`, `/governance`,
    `/ai-agents`, README, .gitignore (секреты исключены).

- **2026-06-22 (сессия 1, Этап 1 — Фундамент, часть 1/1 ✅):**
  - Вынесены нормативные документы в `/docs`: `CONSTITUTION.md` (по статьям),
    `PRINCIPLES.md` (принципы + запреты буквально), `PRIORITIES.md` (приоритет
    распределения + машиночитаемый JSON), `ANTI-ABUSE.md` (механизмы защиты +
    требования к контрактам и агентам). Все кросс-ссылаются, на них будут
    ссылаться контракты (Этап 5) и агенты (Этап 6).
  - Лицензии: `LICENSE` = AGPL-3.0 (код), `LICENSE-DOCS` = CC-BY-SA-4.0 (доки).
  - README: добавлены ссылки на новые доки и точная секция лицензий.
  - Замечено: `scripts/loop.sh` имеет незакоммиченную правку (`IS_SANDBOX=1`,
    нужна для bypassPermissions под root). Это часть пульса — НЕ трогаю,
    оставил как есть (мандат запрещает править loop.sh). Пульс работает.

- **2026-06-22 (сессия 2, Этап 2 — публичный сайт, часть 1/2 ✅):**
  - `web/index.html` — одностраничный публичный сайт (RU): hero + дисклеймер
    (НЕ инвестиция/НЕ пирамида на видном месте), миссия, направления помощи,
    принципы+запреты (буквально), таблица приоритета распределения, прозрачность,
    «как участвовать». Ссылки на нормативные доки ведут на github.com (рендер md).
  - `web/assets/style.css` — самодостаточный стиль, БЕЗ внешних шрифтов/CDN/
    трекеров (ни одного исходящего запроса — это требование прозрачности).
  - `web/.nojekyll` — отключает Jekyll (отдаём готовый статический HTML).
  - `.github/workflows/pages.yml` — авто-деплой `/web` в GitHub Pages при пуше в
    main (Actions-артефакт). Проверено: HTML парсится, теги сбалансированы,
    единственный локальный asset (style.css) на месте.

- **2026-06-22 (сессия 3, Этап 3 — прозрачность, часть 1/2 ✅):**
  - Публичный **реестр решений** `governance/registry/` — append-only журнал с
    hash-chain (каждая запись ссылается на sha256 предыдущей → подмена истории
    заметна сразу). Реализует конституционный принцип «неизменяемость записей»
    ещё до IPFS.
  - `scripts/registry.py` — инструмент без сторонних зависимостей: `verify`
    (схема + цепочка + индекс), `append` (считает seq/prev_hash/hash),
    `reindex`. Догфудинг: genesis + 2 примерные записи созданы самим тулом.
  - Схемы: `record.schema.json` (конверт), `disbursement.schema.json` (payload
    выплаты — привязан к PRIORITIES.md и ANTI-ABUSE.md: приоритет, поэтапность,
    лимиты, коллективная проверка, апелляция). Приватность: без персональных
    данных заявителей, только псевдонимный `case_id`.
  - `index.json` — машиночитаемый индекс + `head_hash` (отпечаток всего реестра,
    удобно пиннить в IPFS).
  - CI `.github/workflows/registry.yml` — `verify` на каждый push/PR: подмена
    или рассинхрон индекса валят сборку.
  - Проверено «до зелёного»: verify=OK на 3 записях; тест подмены записи в
    середине цепочки → verify exit=1 (поймал), после восстановления → exit=0.

- **2026-06-22 (сессия 4, Этап 3 — прозрачность, часть 2/2 ✅):**
  - `scripts/ipfs_manifest.py` — детерминированный IPFS content-манифест без
    сторонних зависимостей. Вычисляет **настоящие** IPFS CIDv1 (raw, single-block)
    локально, прямо из содержимого: для файла ≤256 KiB CID = base32(0x01 0x55 +
    sha2-256). Подтверждено эталоном (CID пустого файла совпал с канонической
    константой). Значит публичные IPFS-ссылки доступны и проверяемы БЕЗ аккаунта
    пиннера — аккаунт нужен только для persistence.
  - `governance/ipfs/manifest.json` — 6 ключевых файлов (`docs/*.md` +
    `governance/registry/index.json`) с size/sha256/CID + `manifest_fingerprint`.
  - `governance/ipfs/README.md` — как устроен CID и два способа независимой
    проверки (через `ipfs add` и чистым пересчётом `verify`).
  - CI `.github/workflows/ipfs.yml` — `verify` на push/PR: контент и CID не
    разойдутся незаметно.
  - Проверено «до зелёного»: build→verify=OK (6 записей); подмена документа →
    verify exit=1 (поймал size/sha256/CID/fingerprint), восстановление → exit=0.
  - Команды пиннинга для оператора: `python3 scripts/ipfs_manifest.py pin-commands`.

## СЛЕДУЮЩИЙ ШАГ

Этап 3 закрыт (реестр решений + IPFS-манифест). Дальше → **Этап 4 (Governance)**:
- Снапшот-спейс (off-chain голосование): подготовить `governance/snapshot/`
  с конфигом space (`space.json`/README) — стратегии голосования, кворум,
  типы предложений, привязка к конституции (приоритет распределения, апелляции).
  Реальное создание space на snapshot.org требует ENS/домен и кошелёк-контроллер
  → запрос оператору (ниже); макет конфига и схему предложений готовлю заранее.
- Макет Safe multisig 5 хранителей (3 из 5): `governance/safe/` — описание схемы,
  роли хранителей, политика подписей, без реальных адресов (адреса — от оператора).
- Хвост Этапа 2: оператору включить Pages (ниже); затем проверить URL и в README.
- Когда у оператора будет токен пиннера — выполнить `pin-commands`, сверить CID.

## НУЖНО ОТ ОПЕРАТОРА (накапливать сюда запросы, не блокироваться)

- **[Этап 2] Включить GitHub Pages:** Settings → Pages → Build and deployment →
  Source = «GitHub Actions». Единоразово. Без этого workflow `pages.yml` собирает
  артефакт, но публичная ссылка не активируется. Ожидаемый URL:
  `https://public-trust.github.io/public-trust-dao/`.
- **[Этап 3] Аккаунт IPFS-пиннера** (web3.storage / Pinata / nft.storage) +
  API-токен в `.env` (НЕ в репо). Нужен, чтобы запиннить нормативные доки и
  `governance/registry/index.json` и получить постоянные CID. До этого готовлю
  локальный манифест (sha256) — пиннинг включается, как только будет токен.
- Понадобится позже: домен/Cloudflare для сайта (опц.), адреса 5 хранителей для
  Safe multisig, решение по тестовой сети для контрактов (LAUNCH предлагает
  Polygon Amoy testnet).
- **⚠️ [ПУЛЬС/РИСК] Перекрытие сессий из-за паузы 10с.** Operator-bridge снизил
  паузу пульса 120→10с. В сессии 3 наблюдал гонку: параллельная сессия пульса
  делала `git add -A && git commit`, пока я ещё создавал файлы, и подхватывала
  мою незавершённую работу в свои коммиты («operator bridge…», «comms…»). Итог
  не потерян (всё в дереве, реестр verify=зелёный), но коммиты мешаются и
  возможны конфликты/частичные снапшоты. Рекомендация оператору: вернуть паузу
  пульса к ~120с или гарантировать единственную активную builder-сессию (lock/
  flock в loop.sh), чтобы сессии не перекрывались. loop.sh сам не правлю (мандат).
