# СЕКРЕТЫ — никогда в репо
.env
*.env
.env.*
**/secrets*
*.key
*_private*
wallet*.json
keystore/

# логи/состояние пульса
logs/

# артефакты сборки
__pycache__/
*.pyc

# копия статус-артефакта, которую деплой Pages кладёт в web/ на лету
# (единый источник истины — governance/status/run_all_status.json)
web/status.json
