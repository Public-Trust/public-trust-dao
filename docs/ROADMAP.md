[Русский] · [English](en/ROADMAP.md)

# ДОРОЖНАЯ КАРТА И САМОРАЗВИТИЕ — PUBLIC TRUST DAO

> Живой документ. Это «топливо» автономной стройки: когда очередь указаний
> оператора (`comms/INBOX.md`) пуста, агент-строитель берёт **следующий
> незакрытый пункт отсюда**, реализует его и предлагает новые идеи.
> Производный от [`MANIFESTO.md`](MANIFESTO.md), [`CONSTITUTION.md`](CONSTITUTION.md),
> [`PRINCIPLES.md`](PRINCIPLES.md) и плана этапов в `LAUNCH.md`.
>
> Зарегистрирован как решение в публичном реестре:
> [`governance/registry/`](../governance/registry/) (запись `PTD-0005`).
>
> **Всё — TESTNET-first.** Любая работа с деньгами — только после независимого
> аудита, явного одобрения оператора и Safe multisig 3-из-5 живых людей.

---

## Зачем этот документ

Проект должен двигаться, даже когда оператор молчит. Чтобы стройка не
останавливалась и не превращалась в хаотичный набор разрозненных правок, нужен
**единый приоритизированный список идей и задач**, который ведёт сам AI и из
которого сам же берёт работу. Это и есть механизм саморазвития:

```
INBOX пуст?  →  взять верхний открытый пункт ROADMAP  →  реализовать до зелёного
            →  отметить [x]  →  предложить 1–3 новые идеи в «Банк идей»
            →  зарегистрировать значимое решение в реестре
```

Правило закреплено в [`BUILDER.md`](../BUILDER.md) (раздел «Саморазвитие»), чтобы
оно соблюдалось каждую сессию, а не только пока про него помнят.

## Как это работает (процесс)

1. **Источник работы — по приоритету:**
   `INBOX` (указания оператора) → `ROADMAP` (этот файл) → план этапов `LAUNCH.md`.
2. **Один связный пункт за сессию.** Крупный пункт дроби: сделай первую часть,
   остальное оставь открытым с пометкой «следующий шаг».
3. **До рабочего результата.** Реальные файлы/код/тесты «до зелёного», без заглушек.
4. **Отметь сделанное** здесь (`- [x]`), перенеси в «Сделано» с номером сессии.
5. **Пополни банк идей.** Каждую сессию предлагай 1–3 новые идеи (даже сырые) —
   так список не иссякает. Идеи может добавлять и оператор.
6. **Логируй значимые решения** в [реестр](../governance/registry/) (тип `decision`).

## Рельсы саморазвития (границы автономии)

Саморазвитие НЕ снимает рельсы безопасности — оно работает строго внутри них:

- **Деньги/ключи/mainnet** — никогда сам. Только подготовка + явное «го» оператора.
- **Внешние действия от лица проекта** (публикации, рассылки, регистрация
  аккаунтов с капчей/телефоном/KYC) — агент готовит «до кнопки», нажимает оператор.
- **Конституционные запреты буквально** — ни одна идея из роадмапа не может их
  нарушать (нет доходности, нет пирамиды, нет рефералов, нет концентрации власти,
  нет скрытых операций). Идея, противоречащая конституции, не берётся в работу.
- **Двуязычность RU↔EN** в одном коммите для любого нового/изменённого дока.
- **Пульс неприкосновенен** (`loop.sh`/`report.sh`/сервис/`.env`/`logs/` не трогать).

---

## Приоритеты (P0 — выше)

`P0` блокирует/несёт наибольшую ценность · `P1` важно · `P2` желательно · `P3` идея на потом.

### P0 — основной план (этапы LAUNCH.md)

- [x] **Этап 4 — Governance, концепция (КУРС УПРАВЛЕНИЯ):** целевая модель
  «1 человек = 1 голос» (Governor → Timelock → Treasury), мультисиг как исполнитель/
  аварийка, путь децентрализации, анти-Сивилла без концентрации власти, поправки к
  конституции как предложение. → Сделано (сессия 10),
  [`GOVERNANCE.md`](GOVERNANCE.md), `PTD-0007`. Это спецификация-основа для частей 1–2.
- [x] **Этап 4 — Governance, часть 1:** макет Snapshot space (off-chain) в
  `governance/snapshot/` — `space.json`/README по [`GOVERNANCE.md`](GOVERNANCE.md):
  стратегия «1 человек = 1 голос» (не плутократия), кворум, типы предложений,
  привязка к конституции (приоритет распределения, апелляции).
  → Сделано (сессия 18), [`governance/snapshot/`](../governance/snapshot/),
  `PTD-0015` (конфиг + JSON-схема + README RU/EN + валидатор рельс
  `scripts/snapshot_config.py` + CI). Создание space (ENS/контроллер) — запрос оператору.
- [x] **Этап 4 — Governance, часть 2:** макет Safe multisig 5 хранителей (3-из-5)
  в `governance/safe/` — схема, роли (исполнитель/аварийная пауза по
  [`GOVERNANCE.md`](GOVERNANCE.md)), политика подписей, без реальных адресов.
  → Сделано (сессия 16), [`governance/safe/`](../governance/safe/), `PTD-0013`
  (вместе с INBOX #10: конфиг + валидатор рельс + CI).
- [ ] **Этап 5 — Смарт-контракты (каркас):** завести `contracts/` как проект
  (Foundry/Hardhat-конфиг), скелеты Treasury / Disbursement (по
  [`ESCROW-TARGETED-DISBURSEMENT.md`](ESCROW-TARGETED-DISBURSEMENT.md)) /
  Governance / Reputation + первые тесты «до зелёного». Только testnet/локально.
  - [x] Часть 1 (сессия 19): проект `contracts/` (Hardhat + ethers v6 + chai,
    Solidity 0.8.24) + контракт-скелет [`Treasury.sol`](../contracts/contracts/Treasury.sol)
    (release только через executor=мультисиг/Timelock, лимит, аварийная пауза,
    события + registryRef) + 10 тестов «до зелёного» + CI. `PTD-0016`. TESTNET-ONLY.
  - [x] Часть 2 (сессия 20): [`Disbursement.sol`](../contracts/contracts/Disbursement.sol)
    — целевой escrow `open/release/refund/pause`; транш уходит строго поставщику
    из кейса («не даём деньги — оплачиваем нужду»), `refund` в казну, лимит транша,
    только executor двигает, guardian только пауза; 14 тестов «до зелёного» (24/24
    с Treasury); реестровая схема расширена `provider/category/escrow_id`. `PTD-0017`.
  - [ ] Часть 3: `Governance` (Governor → Timelock) + `Reputation` (soulbound-бейдж).
    - [x] Часть 3a (сессия 21): [`Reputation.sol`](../contracts/contracts/Reputation.sol)
      — непередаваемый (soulbound) бейдж участника по [`GOVERNANCE.md`](GOVERNANCE.md)
      §2–§3: «1 человек = 1 голос» (`votingUnits` = 1 + min(points, cap), вес в
      коридоре [1..1+cap]), нет функций перевода (непередаваем by design), `verifier`
      выдаёт/отзывает бейдж, `governor` правит параметры, ни одна роль не двигает
      средства; 11 тестов «до зелёного» (35/35 с Treasury+Disbursement). `PTD-0018`.
    - [x] Часть 3b (сессия 22): [`Governor.sol`](../contracts/contracts/Governor.sol)
      + [`Timelock.sol`](../contracts/contracts/Timelock.sol) по [`GOVERNANCE.md`](GOVERNANCE.md)
      §4–§7: прямое голосование (вес из `Reputation.votingUnits`, кворум/срок/публичный
      подсчёт), исполнение ТОЛЬКО через `Timelock` (Governor средства не двигает),
      `guardian` = аварийное вето (`cancel`), `admin` = разовый bootstrap (`renounceAdmin`),
      параметры только голосованием; 15 тестов «до зелёного» (50/50 со всеми
      контрактами). `PTD-0019`.
    - [x] Часть 3c (сессия 23): [`scripts/deploy.js`](../contracts/scripts/deploy.js)
      разворачивает и связывает контур целиком (Reputation→Timelock→Treasury/Disbursement→Governor;
      executor=Timelock, governor=Governor, `renounceAdmin`, Reputation.governor=Timelock)
      + интеграционный тест [`Integration.test.js`](../contracts/test/Integration.test.js)
      «заявка → голос → Timelock → целевая выплата поставщику через `Disbursement`»;
      +4 теста «до зелёного» (54/54 со всеми контрактами). `PTD-0020`.
  - [ ] Часть 4: прогон на публичном testnet (напр. Polygon Amoy) — сеть/RPC/тестовые
    адреса хранителей согласовать с оператором (ключи через `contracts/.env`).
- [ ] **Платформа фонда на Next.js (INBOX #34):** полноценное web-приложение для
  людей (web первым по [`PRODUCT-INTERFACES.md`](PRODUCT-INTERFACES.md)). Статическая
  сборка (`next export`) под бесплатный GitHub Pages с заделом под будущий сервер.
  Рельсы прозрачности: без сторонних трекеров/CDN, системные шрифты, зависимости
  зафиксированы. RU↔EN, доступность. Витрина `web/` остаётся живой, переход постепенный.
  - [x] Каркас + первый экран (сессия 78): проект `platform/` (Next.js 15, App Router,
    TypeScript, React 19) — главная страница (миссия + дисклеймер + витрина 6 будущих
    экранов), двуязычный интерфейс RU↔EN, светлая/тёмная темы, доступность; сборку
    проверяет CI [`.github/workflows/platform.yml`](../.github/workflows/platform.yml)
    на серверах GitHub (локально диск хоста 100%). `PTD-0075`. TESTNET-ONLY.
  - [x] Экран «Подключить кошелёк» (MetaMask) + понятное объяснение (сессия 79):
    `platform/app/wallet/` + блокчейн-слой `platform/lib/wallet.ts` (EIP-1193 без
    сторонних web3-библиотек) — подключение, показ публичного адреса и сети,
    отключение, честное предупреждение при основной сети; карточка на главной стала
    рабочей ссылкой. `PTD-0076`. TESTNET-ONLY.
  - [x] Экран «Подать заявку на помощь» (сессия 80): `platform/app/apply/` + слой
    данных `platform/lib/application.ts` — анонимная форма по `PRIORITIES.md` с
    обязательным целевым расходом (`ESCROW`), собирает проверяемый черновик в
    браузере; карточка на главной стала рабочей. `PTD-0077`. TESTNET-ONLY.
  - [x] Экран «Открытый журнал» (сессия 81): `platform/app/journal/` + слой данных
    `platform/lib/journal.ts` — список решений (новые сверху) с фильтром и поиском,
    объяснена защита от подмены, показан отпечаток журнала; данные едут снимком из
    реестра (`platform/lib/journal-data.json` через `scripts/sync_journal.py`, CI
    пересобирает перед сборкой) — ничего не подгружается со стороны; карточка на
    главной стала рабочей. `PTD-0078`. TESTNET-ONLY.
  - [x] Экран «Голосование» (сессия 82): `platform/app/voting/` + слой данных
    `platform/lib/voting.ts` — один человек один голос (вес всегда 1), голос нельзя
    купить/продать (`GOVERNANCE.md`); показательные предложения с понятными полосками
    итогов, голос собирается проверяемым черновиком-бюллетенем в браузере и хранится
    только у человека; карточка на главной стала рабочей. `PTD-0079`. TESTNET-ONLY.
  - [x] Экран «Окно казны» (сессия 83): `platform/app/treasury/` + слой данных
    `platform/lib/treasury.ts` — read-only состояние общей казны простым языком
    (всего/неприкосновенный запас/прямая помощь ≥70%/благодарность под потолком),
    полоска состава, уровень обеспеченности, последние движения; потратить нельзя,
    данные показательные; карточка на главной стала рабочей. `PTD-0080`. TESTNET-ONLY.
  - [x] Экран «Проверка личности» (сессия 84): `platform/app/identity/` + слой данных
    `platform/lib/identity.ts` — простым языком как фонд убеждается, что за участником
    один настоящий живой человек, без слежки и без сбора лиц; уровни по действию
    (смотреть L0 / заявка L1 / голос и помощь L2) и выбор способа (круг знакомых /
    паспорт признаков / живость у внешнего сервиса / поручительство), граница
    «уникальность — не власть», проверяемая памятка-черновик в браузере без личных
    данных; карточка на главной стала рабочей. `PTD-0081`. TESTNET-ONLY.
    **Этим закрыта запланированная оператором серия экранов ценности (INBOX #34).**
  - [x] Постоянное меню навигации в шапке (сессия 85): `platform/components/SiteHeader.tsx`
    — логотип ведёт на главную с любого экрана, под шапкой постоянное меню со ссылками
    на «Главную» и все шесть готовых экранов, текущий раздел подсвечен (`aria-current`);
    короткие подписи переиспользуют список экранов из `lib/i18n.ts` (RU↔EN), без новых
    зависимостей. Семь экранов связаны в цельное приложение. `PTD-0082`. TESTNET-ONLY.
  - [x] Страница «О платформе» (сессия 86): `platform/app/about/page.tsx` — единая
    страница-вход простым языком (что это, что важно знать сейчас, с чего начать, честно
    и без слежки, частые вопросы); добавлена в меню шапки и подвал, RU↔EN; контент сведён
    из принятых материалов без новых обещаний. `PTD-0083`. TESTNET-ONLY.
  - [x] Понятная страница «страница не найдена» (404): `platform/app/not-found.tsx`
    — спокойное объяснение и путь назад (на главную / о платформе), RU↔EN. `PTD-0084`.
  - [x] Страница «Что приложение помнит о вас» (Мои данные): `platform/app/privacy/`
    + слой данных `platform/lib/storage.ts` — человек видит ВСЁ, что платформа
    сохранила в его браузере, и может стереть целиком или по записи; ничего не уходит
    наружу. Добавлена в меню и подвал, RU↔EN. `PTD-0085`. TESTNET-ONLY.
  - [x] Экран «Что значат слова — словарь простыми словами» (сессия 87):
    `platform/app/glossary/` + слой данных `platform/lib/glossary.ts` — зеркало
    нормативного [`docs/GLOSSARY.md`](GLOSSARY.md) внутри приложения: ~38 ключевых
    слов проекта по-человечески, сгруппированы по смыслу, живой поиск по термину и
    объяснению, счётчик слов; технический термин — только в скобках (правило понятного
    языка). Добавлен в меню, RU↔EN, без новых зависимостей. `PTD-0086`. TESTNET-ONLY.
  - [x] «Забрать копию» — выгрузка своих данных одним файлом (сессия 90):
    на экране [`platform/app/privacy/`](../platform/app/privacy/) добавлена кнопка
    «Скачать мои данные файлом» — человек может забрать копию всего, что платформа
    сохранила в его браузере, как один файл `public-trust-dao-my-data.json` (право
    на портируемость данных, как в GDPR). Всё в браузере: снимок собирается из тех же
    записей под приставкой `ptd:`, что показаны на экране (`exportAll()` в
    [`lib/storage.ts`](../platform/lib/storage.ts)), и отдаётся через временную ссылку —
    наружу ноль исходящих запросов (рельса прозрачности). Кнопка видна только когда
    есть что выгружать; двуязычно RU↔EN, без новых зависимостей. Побочно: впервые
    зафиксирован lock-файл платформы (`platform/package-lock.json`) — supply-chain
    рельса INBOX #34. Сборка до зелёного (typecheck OK, `next build` 13/13, экспорт OK).
    `PTD-0087`. TESTNET-ONLY.
  - [x] Подключить публикацию платформы на GitHub Pages (сессия 92): `pages.yml`
    одним деплоем выкладывает витрину `web/` в корень и платформу `platform/`
    (Next.js, статический экспорт, `basePath=/<repo>/app`) в подпапку `app/` —
    не ломая живой сайт. Заготовка «до кнопки»: оператору остаётся единоразово
    включить Pages (Source: GitHub Actions), после чего платформа видна по
    `.../app/`. `PTD-0089`. Закрыт последний подпункт серии INBOX #34.
  - [x] Поясняющий экран «Помощь и благодарность за труд» (сессия 93):
    `platform/app/rewards/` — зеркало нормативного `docs/REWARDS-MODEL.md` внутри
    приложения (приём «экран-зеркало», как priorities↔PRIORITIES и glossary↔GLOSSARY):
    простым языком — сначала запас, большая часть остатка всегда на помощь, благодарность
    за труд под потолком в треть; три потока (помощь напрямую поставщику / работа /
    волонтёрство); доли двигает только голосование; каждую выплату проверяют ≥2
    независимых человека + ИИ «Справедливость»/«Аудит»; жёсткие границы (помощь важнее
    награды, нет рефералов/пирамиды). Тексты RU↔EN в `lib/i18n.ts` (`RewardsDict`),
    добавлен в меню, без новых зависимостей. `PTD-0090`. TESTNET-ONLY.
  - [x] Поясняющий экран «Как фонд защищён от обмана и воровства» (сессия 94):
    `platform/app/safeguards/` — зеркало нормативного `docs/ANTI-ABUSE.md` внутри
    приложения (приём «экран-зеркало», как priorities↔PRIORITIES, rewards↔REWARDS-MODEL,
    glossary↔GLOSSARY): простым языком — главная мысль «быть честным выгоднее, чем
    обманывать»; 7 пунктов «Главное» (по частям / лимит на крупное / подтверждение
    несколькими независимыми / открытый журнал / стоп-кран без вывода себе / заслуги не
    дают власти над деньгами / право оспорить); 9 опор защиты кошелька; про репутацию
    (только подспорье, нельзя купить, не зависит от «приведи друга», без власти над
    деньгами). Тексты RU↔EN в `lib/i18n.ts` (`SafeguardsDict`), добавлен в меню, без
    новых зависимостей. Опирается на конституцию ст.7. `PTD-0091`. TESTNET-ONLY.
  - [x] Поясняющий экран «Как фонд создаёт оплачиваемую работу — и честно проверяет,
    что она сделана» (сессия 95): `platform/app/work/` — зеркало нормативного
    `docs/PROOF-OF-CONTRIBUTION.md` внутри приложения (приём «экран-зеркало», как
    priorities↔PRIORITIES, rewards↔REWARDS-MODEL, safeguards↔ANTI-ABUSE,
    glossary↔GLOSSARY): простым языком — главная мысль «фонд не только раздаёт помощь,
    а создаёт честную работу вокруг неё; за работу платят, но не на слово»; ключевой
    вопрос доверия; 6 пунктов «Главное» (деньги заранее заморожены под задачу /
    получить только доказав / проверяющие рискуют своим именем / окно на спор / всё на
    виду / сначала помощь, потом награда); два формата работы (разовая задача /
    этапная); три опоры доказательства (подтверждение того, для кого сделано + следы-
    отпечатки без публикации файлов + проверка нескольких независимых людей); честно о
    слабом месте оракула; жёсткие границы. Тексты RU↔EN в `lib/i18n.ts` (`WorkDict`),
    добавлен в меню, без новых зависимостей. Опирается на конституцию ст.5/6/7.
    `PTD-0092`. TESTNET-ONLY.
  - [x] Поясняющий экран «Всё под подписью и прослеживаемо» (сессия 96):
    `platform/app/accountability/` — зеркало нормативного `docs/ACCOUNTABILITY.md`
    внутри приложения (приём «экран-зеркало», как rewards↔REWARDS-MODEL,
    safeguards↔ANTI-ABUSE, work↔PROOF-OF-CONTRIBUTION, glossary↔GLOSSARY): простым
    языком — главная мысль «видно, кто сделал; видно, что сделал; и переписать это
    задним числом нельзя»; 6 пунктов «Главное» (у каждого действия есть автор /
    подписано = под ответственностью / прошлое не перепишешь / в блокчейн только важное,
    отпечаток а не личные данные / ИИ под подписью, но без власти / разработчики тоже
    под подписью); два уровня «под подписью» / «в блокчейне»; кто за что отвечает
    (участник/проверяющий/ИИ/разработчик/хранитель 3-из-5); «действия ИИ всегда видно,
    но без власти» (ст.9); граница «подпись = ответственность, не власть над деньгами».
    Тексты RU↔EN в `lib/i18n.ts` (`AccountabilityDict`), добавлен в меню, без новых
    зависимостей. Опирается на конституцию ст.2/3/9. `PTD-0093`. TESTNET-ONLY.
  - [x] Поясняющий экран «Как фонд оплачивает нужду напрямую» (сессия 99):
    `platform/app/direct-help/` — зеркало нормативного `docs/ESCROW-TARGETED-DISBURSEMENT.md`
    внутри приложения (приём «экран-зеркало», как priorities↔PRIORITIES,
    rewards↔REWARDS-MODEL, safeguards↔ANTI-ABUSE, work↔PROOF-OF-CONTRIBUTION,
    accountability↔ACCOUNTABILITY, glossary↔GLOSSARY): простым языком — главный принцип
    помощи «мы не даём деньги — мы оплачиваем нужду, и каждый может это проверить»; 5
    пунктов «Главное» (деньги прямо поставщику, не на руки / подтверждение встроено само /
    это не слежка: открыт платёж, а не личность / сорвалось — деньги возвращаются в фонд /
    длинную помощь платим по частям); три способа оплатить нужду (заморозка / проверенный
    поставщик / вне сети с отпечатком); таблица «что открыто (платёж) ↔ что приватно
    (личность)»; граница «оплата нужды, НЕ инвестиция; никто не двигает деньги в одиночку».
    Тексты RU↔EN в `lib/i18n.ts` (`EscrowDict`), добавлен в меню и связан ссылкой из экрана
    «Подать заявку», без новых зависимостей. Опирается на конституцию ст.4/5/7. `PTD-0096`. TESTNET-ONLY.
  - [x] Экран «Конституция фонда — простыми словами» (сессия 101):
    `platform/app/constitution/` — зеркало нормативного `docs/CONSTITUTION.md`
    внутри приложения (приём «экран-зеркало», как priorities↔PRIORITIES,
    rewards↔REWARDS-MODEL, safeguards↔ANTI-ABUSE, work↔PROOF-OF-CONTRIBUTION,
    accountability↔ACCOUNTABILITY, direct-help↔ESCROW, glossary↔GLOSSARY): north star
    проекта теперь объяснён внутри платформы, а не только внешней ссылкой на GitHub.
    Главная мысль (высший закон, всё ему подчиняется); все 10 статей пересказаны
    по-человечески; блок «неизменяемое ядро» (ст. 1, 2, 3 и п. 6.2 нельзя отменить даже
    голосованием); граница «действует уже сейчас, всё на тестовой сети без реальных денег».
    Тексты RU↔EN в `lib/i18n.ts` (`ConstitutionDict`), добавлен в меню после «О платформе»,
    без новых зависимостей. Опирается на конституцию ст.1–10. `PTD-0098`. TESTNET-ONLY.
  - [x] Экран «Как фонд принимает решения: голос каждого человека» (сессия 102):
    `platform/app/governance/` — зеркало нормативного `docs/GOVERNANCE.md` внутри
    приложения (тот же приём «экран-зеркало»). Простым языком: главная мысль («чем шире
    право решать, тем труднее захватить фонд»); 6 простых правил управления; «хранители —
    исполнитель, не начальник» (3-из-5, аварийный тормоз); защита от накруток без сбора
    лиц/слежки + запасной путь поручительства; путь к самостоятельности; граница
    «уникальность это не власть»; экран-ОБЪЯСНЕНИЕ со ссылкой на бюллетень `/voting/`.
    Тексты RU↔EN в `lib/i18n.ts` (`GovernanceDict`), добавлен в меню после «Конституции»,
    без новых зависимостей. Опирается на CONSTITUTION ст.2/3/4. `PTD-0099`. TESTNET-ONLY.
  - [x] Экран «Манифест фонда — простыми словами» (сессия 103):
    `platform/app/manifesto/` — зеркало нормативного `docs/MANIFESTO.md` внутри
    приложения (тот же приём «экран-зеркало»). Простым языком: зачем существует фонд
    (взаимопомощь, общественное благо, не вложение денег); 11 направлений помощи
    человеческими словами; «за полезный труд благодарят, но НИКОГДА не за привлечение
    людей»; 6 конституционных запретов; «у фонда нет владельца»; роль ИИ (служит, без
    власти и без денег); высшая цель (получивший помощь со временем сам помогает).
    Тексты RU↔EN в `lib/i18n.ts` (`ManifestoDict`), добавлен в меню перед «Конституцией»,
    без новых зависимостей. Опирается на MANIFESTO/CONSTITUTION. `PTD-0100`. TESTNET-ONLY.
    **Серия экранов-зеркал нормативных доков почти закрыта — остался `SUPPORT-MODEL.md`.**
- [x] **Этап 6 — AI-агенты (каркас): ЗАВЕРШЁН (8/8, сессия 31).** В `ai-agents/`
  заведены все восемь модулей-помощников соблюдения конституции (служебные модули,
  не органы власти — ст. 9; read-only к средствам, находка = сигнал, не действие),
  каждый «до зелёного» и с тест-инвариантом, все в CI `ai-agents.yml`:
  Audit, Guardian, Fairness, Reputation, Housing, Documentation, Governance, Mediator.
  - [x] Модуль 1/8 (сессия 24): каркас [`ai-agents/`](../ai-agents/) (README RU/EN
    с принципом «ИИ служит, не правит» и таблицей 8 агентов) + первый рабочий агент
    **Audit** [`audit_agent.py`](../ai-agents/audit_agent.py) — объединяет 4 рельс-
    валидатора (registry/ipfs/safe/snapshot) в один аудит-прогон с привязкой каждой
    проверки к статье конституции, опция `--with-contracts`, вывод человекочитаемый
    и `--json`; CI [`ai-agents.yml`](../.github/workflows/ai-agents.yml). «До зелёного»:
    4/4 базовый, 5/5 с контрактами (54 теста). `PTD-0021`. Реализует и идею «единый
    governance-валидатор в CI». TESTNET-ONLY.
  - [x] Модуль 2/8 (сессия 25): **Guardian** [`guardian_agent.py`](../ai-agents/guardian_agent.py)
    — отдельный явный сканер рельс безопасности по ВСЕМУ дереву репо (git-отслеживаемые
    файлы): нет закоммиченных секретов/ключей (`secrets-tracked`), `.gitignore` прикрывает
    `.env`/`logs/` (`gitignore-guards`), нет mainnet `chain_id` (`no-mainnet`), нет
    приватных ключей в тексте (`no-key-material`, 64-hex вне хэш-полей + присваивание
    секрета). Тест-инвариант [`test_guardian.py`](../ai-agents/test_guardian.py) (14/14)
    доказывает «красное ловится, зелёное не ложно-падает». CI расширен. `PTD-0022`.
  - [x] Модуль 3/8 (сессия 26): **Fairness** [`fairness_agent.py`](../ai-agents/fairness_agent.py)
    — read-only проверка справедливости распределения над записями реестра типа
    `disbursement` по [`PRIORITIES.md`](PRIORITIES.md)/[`ANTI-ABUSE.md`](ANTI-ABUSE.md):
    `priority-valid` (уровень в шкале 1..10, читается прямо из PRIORITIES.md),
    `safeguards` (приоритет не отключает лимит/проверку/окно апелляции), `collective-review`
    (≥2 независимых подтверждения), `staged-payments` (поэтапность), `applicant-privacy`
    (нет персональных данных). Тест-инвариант [`test_fairness.py`](../ai-agents/test_fairness.py)
    (17/17). Побочно исправлен латентный баг Guardian (ложно краснел на собственном
    `test_guardian.py`) — CI `ai-agents` снова зелёный. `PTD-0023`.
  - [x] Модуль 4/8 (сессия 27): **Reputation** [`reputation_agent.py`](../ai-agents/reputation_agent.py)
    — read-only статический разбор `contracts/contracts/Reputation.sol` и
    `governance/snapshot/space.json`: доказывает «1 человек = 1 голос» по
    [`GOVERNANCE.md`](GOVERNANCE.md) §2–§3 В КОДЕ — `soulbound` (нет функций
    перевода), `bounded-weight` (вес 0 / `1 + min(points, cap)`, коридор [1..1+cap]),
    `no-funds` (слой репутации не двигает средства), `roles-separated` (verifier
    выдаёт/отзывает бейдж, governor правит параметры), `off-chain-equal` (Snapshot =
    равный `ticket` value=1, не плутократия). Тест-инвариант
    [`test_reputation.py`](../ai-agents/test_reputation.py) (17/17). `PTD-0024`.
  - [x] Модуль 5/8 (сессия 28): **Housing** [`housing_agent.py`](../ai-agents/housing_agent.py)
    — профильный помощник по жилищным кейсам. Read-only доказывает модель целевого
    расхода (docs/ESCROW-TARGETED-DISBURSEMENT.md) «помощь оплачивается напрямую
    поставщику, не на руки» В КОДЕ `contracts/contracts/Disbursement.sol`:
    `release-to-provider-only` (release без адреса-параметра → транш строго на
    c.provider), `provider-fixed` (нет setProvider/.provider=), `refund-to-treasury`
    (возврат в казну, не получателю), `tranche-limit` (потолок maxRelease),
    `guardian-cannot-move` (средства двигает только executor) + проверки жилищных
    записей реестра (`targeted-escrow`/`provider-onchain`/`category-priority`,
    уровень читается из PRIORITIES.md). Тест-инвариант
    [`test_housing.py`](../ai-agents/test_housing.py) (23/23). CI расширен (+ триггеры
    на contracts/contracts и docs/PRIORITIES.md). `PTD-0025`.
  - [x] Модуль 6/8 (сессия 29): **Documentation** [`documentation_agent.py`](../ai-agents/documentation_agent.py)
    — read-only проверка двуязычности RU↔EN и целостности ссылок по git-отслеживаемым
    `.md`: `bilingual-pairs` (у каждого публичного дока есть пара по правилу пар из
    пути), `language-switcher` (вверху корректный переключатель `[Русский]·[English]`
    к парному файлу), `link-integrity` (все относительные ссылки резолвятся; внешние
    ссылки и блоки кода исключены). Тест-инвариант
    [`test_documentation.py`](../ai-agents/test_documentation.py) (17/17). При первом
    прогоне вскрыл и закрыл реальный пробел — добавлены EN-зеркала
    `governance/ipfs/README.md` и `governance/registry/README.md`. Закрывает и P2
    «авто-проверка двуязычности в CI». `PTD-0026`.
  - [x] Модуль 7/8 (сессия 30): **Governance** [`governance_agent.py`](../ai-agents/governance_agent.py)
    — read-only проверка ЖИЗНЕННОГО ЦИКЛА ПРЕДЛОЖЕНИЯ из `GOVERNANCE.md` над конфигами
    управления `governance/snapshot/space.json` и `governance/safe/safe.config.json`
    (сам НЕ голосует): `one-person-one-vote` (стратегия `ticket` value=1, не плутократия),
    `timed-vote` (`delay`/`period` > 0), `off-chain-signal` (`off_chain_signaling=true` и
    типы `binding=false`), `proposal-binding` (`disbursement-direction`→PRIORITIES+ANTI-ABUSE;
    `constitution-amendment`→CONSTITUTION+`requires_supermajority`), `multisig-not-sole`
    (порог ≥2 и < числа владельцев, 3-из-5), `lifecycle-links` (ссылки конфигов резолвятся).
    Тест-инвариант [`test_governance.py`](../ai-agents/test_governance.py) (26/26). CI
    расширен. На реальных конфигах: 6/6. `PTD-0027`.
  - [x] Модуль 8/8 — последний (сессия 31): **Mediator** [`mediator_agent.py`](../ai-agents/mediator_agent.py)
    — read-only проверка процесса споров/апелляций (артефакт
    [`governance/mediation/dispute-process.json`](../governance/mediation/dispute-process.json))
    по `ANTI-ABUSE.md` §6: `appeal-for-every-sanction` (у каждой санкции — отказ/
    репутация/заморозка/исключение — есть обжалование), `mediator-not-decider`
    (решают люди ≥2, не ИИ/один), `independent-review` (апелляцию решает не автор
    санкции), `valid-lifecycle` (один старт/терминал/без тупиков/всё достижимо),
    `bounded-timelines` (сроки > 0), `process-links` (ссылки резолвятся);
    **СТРУКТУРИРУЕТ, НЕ решает** (ст. 9.2). Тест-инвариант
    [`test_mediator.py`](../ai-agents/test_mediator.py) (26/26). CI расширен.
    На реальном артефакте: 6/6. **Закрыл каркас всех восьми агентов (8/8).** `PTD-0028`.

### P1 — материалы и инфраструктура (часть — из INBOX)

- [x] **Продвижение (INBOX #6):** лендинг для людей, короткий питч RU/EN,
  пост-анонс, FAQ «это общественное благо, НЕ инвестиция». Готовит агент;
  публикацию делает оператор. → Сделано (сессия 9), [`PROMOTION.md`](PROMOTION.md), `PTD-0006`.
- [x] **Почта проекта (INBOX #7):** инструкция оператору — варианты домена+почты
  (ProtonMail/собственный домен), тексты. Регистрацию делает оператор.
  → Сделано (сессия 14), [`EMAIL-SETUP.md`](EMAIL-SETUP.md), `PTD-0011`.
- [x] **Testnet-кошелёк + Safe (INBOX #10):** описать/создать тестовый кошелёк и
  Safe для тестовой казны (без реальных денег и приватных ключей в репо), адреса
  задокументировать открыто. → Сделано (сессия 16), [`governance/safe/`](../governance/safe/),
  `safe.config.json` + `scripts/safe_config.py` (валидатор рельс) + CI, `PTD-0013`.
- [x] **Аутрич-шаблоны (INBOX #8):** список контактов + шаблоны писем RU/EN под
  миссию «общественное благо». Отправляет оператор лично.
  → Сделано (сессия 15), [`OUTREACH.md`](OUTREACH.md), `PTD-0012`.
- [x] **Модель поддержки/донатов (INBOX):** поддержка через саму работающую
  систему (прозрачная казна-мультисиг + контракты, видна в реестре), без
  отдельной кнопки «Поддержать»; адрес для реальных денег — только после запуска
  и аудита. → Сделано (сессия 17), [`SUPPORT-MODEL.md`](SUPPORT-MODEL.md), `PTD-0014`.

### P2 — качество и прозрачность

- [x] **CONTRIBUTING.md (+EN)** — как стороннему человеку участвовать (issues, PR,
  предложения в governance), кодекс поведения, ссылки на конституцию.
  → Сделано (сессия 34), [`CONTRIBUTING.md`](../CONTRIBUTING.md), `PTD-0031`.
  Акцент «вклад ≠ власть над казной» (1 человек = 1 голос; авторство = признание).
- [x] **Глоссарий** ключевых терминов (DAO, escrow, multisig, реестр, приоритет
  распределения) простым языком, RU/EN — чтобы документы были понятны не-техническим.
  → Сделано (сессия 36), [`GLOSSARY.md`](GLOSSARY.md), `PTD-0033`. ~40 терминов в
  5 группах (основы / управление / деньги и помощь / прозрачность / безопасность),
  каждый со ссылкой на нормативный док; финал «три вещи держать в голове».
- [x] **CODE_OF_CONDUCT.md (+EN)** — вынести кодекс поведения из CONTRIBUTING в
  отдельный канонический файл (GitHub его распознаёт и показывает на странице
  «Community»), со ссылкой на конституцию/принципы (предложено сессией 34).
  → Сделано (сессия 35), [`CODE_OF_CONDUCT.md`](../CODE_OF_CONDUCT.md), `PTD-0032`.
  Ключ: нарушение рельс безопасности = нарушение кодекса; решения принимают люди
  (не агент, ст. 9); приватность заявителей/подопечных; атрибуция Contributor Covenant 2.1.
- [x] **Шаблоны issue/PR (.github/ISSUE_TEMPLATE, PULL_REQUEST_TEMPLATE)** —
  направить вклад по форматам из CONTRIBUTING (баг/идея/governance-предложение;
  чек-лист рельс: двуязычность, без секретов, TESTNET) — заготовка «до кнопки»
  для стороннего участника (предложено сессией 34).
  → Сделано (сессия 37), `.github/ISSUE_TEMPLATE/` (config + 4 YAML-формы) +
  `.github/pull_request_template.md` (двуязычный), `PTD-0034`. Все формы
  двуязычны и с чек-листами конституционных рельс; формы issue как YAML
  (структурированы, не попадают в .md-скан); шаблон PR добавлен в SINGLE_LANG
  Documentation-агента (GitHub-форма, двуязычная внутри себя).
- [x] **Страница «Прозрачность» на сайте** — собрать ссылки: реестр, IPFS-манифест,
  CI-статусы, как самому проверить целостность (`registry.py verify`).
  → Сделано (сессия 38), [`web/transparency.html`](../web/transparency.html) (+EN),
  `PTD-0035`. Статусы CI даны ссылками, не картинками-бейджами — ноль исходящих запросов.
- [x] **Авто-проверка двуязычности в CI** — линтер, который валит сборку, если
  RU-док без парного EN (или наоборот), нет переключателя языка или битая ссылка.
  → Реализовано Documentation-агентом (сессия 29):
  [`ai-agents/documentation_agent.py`](../ai-agents/documentation_agent.py), `PTD-0026`.

### P3 — банк идей (сырые, обсуждаемые)

- [ ] **Перекрёстные ссылки между экранами-зеркалами платформы** — серия экранов
  (`manifesto`, `constitution`, `governance`, `priorities`, `rewards`, `safeguards`,
  `work`, `accountability`, `direct-help`, `support`, `glossary`) закрыта (сессия 104,
  `PTD-0101`); теперь связать их человеческими ссылками «см. также» внутри текстов
  (напр. со «Поддержать проект» → на «Оплата напрямую» и «Как решаем»), чтобы человек
  ходил между объяснениями без возврата на главную. Чисто-фронтенд, без новых
  зависимостей (предложено сессией 104).
- [ ] **Карточка «Поддержать проект» на главной платформы** — экран `support` есть в
  меню, но на главной (`platform/app/page.tsx`) о нём нет карточки; добавить понятную
  карточку «Как поддержать проект» в витрину, чтобы тема честной поддержки была видна
  сразу. Без денежных адресов (рельс — адрес не публикуется до аудита). Предложено
  сессией 104.
- [ ] **Контракт-адаптер проверки личности (`PersonhoodVerifier`)** — на основе
  `docs/IDENTITY-VERIFICATION.md`: тонкий контракт, который принимает доказательство
  одного из разрешённых методов proof-of-personhood и вызывает `Reputation.mint`
  только при `verified ∧ уникальный (nullifier не использован)`; allow-list методов —
  под `Timelock`. Заземляет «уникальность ≠ власть» в код (предложено сессией 44).
- [x] **Термины проверки личности в `GLOSSARY.md`** (+EN) — добавить простыми
  словами: «proof-of-personhood», «zero-knowledge / доказательство без раскрытия»,
  «nullifier», «liveness», «поручительство (web-of-trust)» — со ссылкой на
  `IDENTITY-VERIFICATION.md` (предложено сессией 44). → Сделано (сессия 53),
  новая группа «Проверка личности — что за аккаунтом живой человек» в
  [`GLOSSARY.md`](GLOSSARY.md) (+EN): 6 терминов простыми словами (защита от накруток/
  Сивиллы, proof-of-personhood, liveness, zero-knowledge, nullifier, поручительство),
  каждый со ссылкой на [`IDENTITY-VERIFICATION.md`](IDENTITY-VERIFICATION.md). `PTD-0050`.
- [x] **Ссылка на глоссарий в шапке нормативных доков** — добавить в шапку каждого
  дока ссылку на [`GLOSSARY.md`](GLOSSARY.md), чтобы непонятный термин был в один
  клик от любого документа (предложено сессией 36). → Сделано (сессия 57): в шапку
  шести нормативных доков (GOVERNANCE / ANTI-ABUSE / REWARDS-MODEL /
  PROOF-OF-CONTRIBUTION / ESCROW-TARGETED-DISBURSEMENT / PRIORITIES, RU + EN)
  добавлен указатель «Непонятные технические слова … объяснены простым языком в
  [глоссарии](GLOSSARY.md) — группа «…»» с точным названием группы; `PTD-0054`.
  Связь с глоссарием теперь двусторонняя для всех нормативных доков.
- [x] **Documentation-агент: мягкая проверка «термин определён»** — лёгкий
  словарный линтер, который для списка ключевых терминов (DAO/escrow/мультисиг/…)
  проверяет, что у проекта есть статья в `GLOSSARY.md`; не блокирует, а
  предупреждает, чтобы глоссарий не отставал от документов (предложено сессией 36).
  **Сделано (сессия 61):** проверка `glossary-coverage` в `documentation_agent.py`
  (`soft=True`, список `KEY_TERMS` из 26 терминов, RU+EN; вердикт/`passed`/`total`
  считаются только по блокирующим проверкам, добавлено поле `warnings`); 2 новых
  тест-сценария (23/23); таблица проверок в README агента (+EN). `PTD-0058`.
- [x] **Documentation-агент: мягкая проверка «нет мёртвых статей глоссария»** —
  обратная к `glossary-coverage`: предупреждать, если статья в `GLOSSARY.md` есть,
  но сам термин нигде в нормативных документах не встречается (глоссарий «оброс»
  лишним). Тоже мягкая, не блокирует (предложено сессией 61, развитие `PTD-0058`).
  **Сделано (сессия 62):** проверка `glossary-no-dead` в `documentation_agent.py`
  (`soft=True`): парсер заголовков статей (полностью-жирная строка с точкой —
  отсекает выделение **жирным** в теле), консервативное извлечение ключей (основная
  фраза + варианты из скобок + дробление по союзам/кавычкам + пословный фолбэк
  значимыми словами) — на реальном репо 0 ложных тревог; 2 новых тест-сценария
  (28/28); строка в таблице проверок README агента (+EN). `PTD-0059`.
- [x] **Каталог GitHub-меток `.github/labels.yml`** — единый набор меток (`bug`,
  `idea`, `governance`, `safety`, …), на которые ссылаются формы issue, чтобы
  цвета/описания меток были воспроизводимы и заводились не вручную (предложено сессией 37).
  **Сделано (сессия 76):** `.github/labels.yml` — единый каталог (RU/EN описания,
  hex-цвета): 4 метки форм (`bug`/`idea`/`governance`/`safety`) + `documentation`/
  `question` + слои `area:*` + статусы (`good first issue`/`help wanted`/`blocked`/
  `duplicate`/`wontfix`); формат совместим с обычными синхронизаторами меток;
  применение к реальному репо — за оператором (внешнее действие). Десятая проверка
  стража структуры `issue-form-labels-defined` (hard) краснеет, если форма ссылается
  на метку не из каталога; `test_structure_guard.py` 98/98. `PTD-0073`.
- [x] **Опциональный синхронизатор меток** — заготовка workflow/инструкции, которая
  по `.github/labels.yml` приводит метки реального репозитория в соответствие с
  каталогом (например `EndBug/label-sync`). Включение и применение к реальному репо —
  за оператором (внешнее действие от лица проекта); агент готовит «до кнопки» (сессия 76).
  **Сделано (сессия 77):** `.github/workflows/labels.yml` — рабочий процесс синхронизации
  меток через `EndBug/label-sync@v2` по конфигу `.github/labels.yml`. Триггер ТОЛЬКО
  ручной (`workflow_dispatch`, без push) — внешнее действие выполняет оператор; по
  умолчанию предпросмотр (`dry_run=true`, ничего не меняет), удаление чужих меток
  отключено (`delete_others=false`). Раздел «Как применить» каталога дополнен ссылкой
  на процесс. Замыкает петлю каталог→страж→синхронизация. `PTD-0074`.
- [ ] **Страж: каталог меток без дублей и с валидным hex-цветом** — мягкая проверка
  `.github/labels.yml`: имена меток уникальны, `color` — ровно 6 hex-символов без `#`,
  у каждой метки есть описание (предложено сессией 76, развитие `issue-form-labels-defined`).
- [ ] **Страж: процесс синхронизации меток остаётся ручным** — мягкая проверка, что
  `.github/workflows/labels.yml` имеет триггер `workflow_dispatch` и НЕ имеет триггера
  `push` (внешнее действие должно остаться за оператором, рельс не должен «уехать»
  при будущих правках). Предложено сессией 77, развитие `PTD-0074`.
- [x] **Documentation-агент: симметрия глоссария RU↔EN** — мягкая проверка, что
  число статей в `docs/GLOSSARY.md` и `docs/en/GLOSSARY.md` совпадает (глоссарии не
  разъехались по объёму). Предупреждение, не блок (развитие `PTD-0059`, сессия 62).
  **Сделано (сессия 100):** добавлена мягкая проверка `glossary-symmetry` в
  `documentation_agent.py` — сравнивает число статей (полностью-жирных строк-заголовков)
  RU↔EN и при расхождении сообщает, в каком языке статей больше и на сколько (перенос
  термина в зеркало мог потеряться). Тест-инвариант расширен (48→54 проверки, зелёные):
  асимметрия ловится, равное число не даёт ложной тревоги. На реальном репо: зелёно,
  RU=EN=44. Зарегистрировано `PTD-0097`.
- [ ] **Documentation-агент: каждая статья глоссария ведёт к нормативному доку** —
  мягкая проверка, что у статьи глоссария есть ссылка `См. [...]` хотя бы на один
  документ `docs/`/`governance/` (где термин закреплён формально), чтобы глоссарий
  оставался «производным», а не самостоятельным источником (сессия 62).
  ⚠️ **Уточнение (сессия 100):** проверять КАЖДУЮ статью на ссылку — пока преждевременно:
  на реальном глоссарии 26 из 44 статей ссылки не имеют, и сам глоссарий во введении
  оговаривает ссылку лишь «**где уместно**» (общие термины — DAO, хэш, CID, приватный
  ключ — закрепляются не одним документом). Производность глоссария уже обеспечена на
  уровне ДОКУМЕНТА (шапка ссылается на CONSTITUTION/PRINCIPLES/GOVERNANCE/PRIORITIES/
  ANTI-ABUSE). Чтобы пункт не стал шумом из 26 предупреждений, его следует либо
  переформулировать как «доля статей со ссылкой ≥ порога», либо сперва обогатить
  глоссарий ссылками — отдельной задачей-наполнением, не линтером.
- [x] **SECURITY.md (+EN)** — канонический файл политики безопасности (GitHub
  распознаёт и показывает кнопку «Report a vulnerability»): куда и как сообщать о
  дыре в рельсах/контрактах, что в зоне ответственности (только testnet), чего НЕ
  обещаем (нет программы вознаграждений за реальные деньги до аудита), приватный
  канал = тот же `security@` из [`EMAIL-SETUP.md`](EMAIL-SETUP.md). Дополняет
  CODE_OF_CONDUCT (нарушение рельс = нарушение поведения) и Guardian-агента
  (предложено сессией 35). **Сделано (сессия 39):** `SECURITY.md` (+EN), кросс-линки
  из CODE_OF_CONDUCT/README/REPO-STRUCTURE, safe-harbor, coordinated disclosure,
  `PTD-0036`.
- [x] **Мета-агент «прогнать всех»** `ai-agents/run_all.py` — единая точка входа,
  которая запускает все восемь агентов и сводит их `--json`-отчёты в один
  «зелёный/красный» с разбивкой по агентам/проверкам. Упростит CI (один шаг вместо
  16) и self-check контура (предложено сессией 31, после закрытия каркаса 8/8).
  → Сделано (сессия 32), [`ai-agents/run_all.py`](../ai-agents/run_all.py) +
  тест-инвариант [`test_run_all.py`](../ai-agents/test_run_all.py) (13/13), CI сжат
  с ~15 шагов до 2, `PTD-0029`.
- [x] **Рефакторинг общих solidity-помощников** в `ai-agents/` — статический разбор
  `.sol` дублировался (Reputation/Housing); вынесен в общий модуль
  [`solidity_scan.py`](../ai-agents/solidity_scan.py) с тестом, чтобы агенты не
  расходились в логике парсинга (предложено сессией 31). → Сделано (сессия 40),
  `PTD-0037`: `strip_solidity_comments`/`has_function`/`function_signature`/
  `function_body` в одном модуле, оба агента импортируют его; тест-инвариант
  [`test_solidity_scan.py`](../ai-agents/test_solidity_scan.py) (14/14), включён в
  `run_all` (тесты 9/9). Поведение агентов не изменилось (Reputation 5/5, Housing 8/8).
- [x] **Страж структуры: проверка `run_all` покрывает всех** — расширить
  [`structure_guard.py`](../ai-agents/structure_guard.py) проверкой, что список `TESTS`
  в `run_all.py` действительно включает каждый существующий `test_*.py` (и наоборот,
  каждый `AGENTS`-ключ имеет агента) — чтобы новый агент/тест нельзя было завести в
  обход общего прогона CI (продолжение стража, сессия 51).
  → Сделано (сессия 52) проверкой `run-all-covers-all` стража
  [`structure_guard.py`](../ai-agents/structure_guard.py), `PTD-0049`. Заодно закрыт
  реальный пробел: `test_run_all.py` добавлен в `TESTS` (мета-агент не прогонял свой
  тест-инвариант; тесты 10→11).
- [x] **Страж структуры: CI реально зовёт `run_all`** — добавить проверку, что
  `.github/workflows/ai-agents.yml` действительно запускает `run_all.py --with-tests`
  (а не отдельные agent-команды в обход) — чтобы покрытие нельзя было обойти уже на
  уровне CI-воркфлоу, а не только списков `AGENTS`/`TESTS` (продолжение
  `run-all-covers-all`, сессия 52).
  → Сделано (сессия 60) пятой проверкой `ci-calls-run-all` стража
  [`structure_guard.py`](../ai-agents/structure_guard.py): читает сам файл воркфлоу и
  краснеет, если тот не зовёт `run_all.py` с `--with-tests` (агенты в обход, без
  тест-инвариантов или воркфлоу нет вовсе). `test_structure_guard.py` 34/34,
  страж зелёный 5/5, `PTD-0057`.
- [x] **Страж структуры: триггер-пути воркфлоу включают `ai-agents/**`** — проверить,
  что в `.github/workflows/ai-agents.yml` блок `on.push.paths` (и `pull_request.paths`)
  действительно содержит `ai-agents/**`, иначе изменение агента не запустит CI и
  покрытие обходится ещё раньше — на уровне триггера, а не команды (продолжение
  `ci-calls-run-all`, сессия 60). → Сделано (сессия 63) шестой проверкой
  `trigger-paths-include-agents` стража [`structure_guard.py`](../ai-agents/structure_guard.py)
  (лёгкий парсер YAML по отступам, без зависимостей): требует `ai-agents/**` в обоих
  триггерах, краснеет при отсутствии пути или блока. `test_structure_guard.py` 43/43,
  страж зелёный 6/6, `PTD-0060`. Замкнул защиту покрытия от обхода на всех уровнях
  (списки → вызов в CI → срабатывание триггера). Заодно догнаны README агента (RU/EN):
  отставшая строка `ci-calls-run-all` + новая строка.
- [x] **Страж структуры: воркфлоу `ai-agents.yml` зовёт и `test_run_all.py`** — добавить
  проверку, что отдельный шаг воркфлоу запускает тест-инвариант мета-агента
  `test_run_all.py` (а не только `run_all.py --with-tests`) — он доказывает, что само
  сведение «красное → красное» исправно; сейчас это держится на дисциплине воркфлоу
  (предложено сессией 63; продолжение `trigger-paths-include-agents`).
  → Сделано (сессия 64) седьмой проверкой `ci-runs-test-run-all` стража
  [`structure_guard.py`](../ai-agents/structure_guard.py): читает воркфлоу и краснеет,
  если тот не прогоняет `test_run_all.py` отдельным шагом. `test_structure_guard.py`
  50/50, страж зелёный 7/7, `PTD-0061`. Достроил защиту покрытия от обхода до самого
  верха (списки → вызов в CI → срабатывание триггера → прогон тест-инварианта сведения).
- [x] **Страж структуры: воркфлоу проверяется на полноту шагов CI** — обобщить
  `ci-calls-run-all`/`ci-runs-test-run-all`: вместо проверки отдельных команд по одной
  держать в страже единый список «обязательных команд воркфлоу» и краснеть при
  пропаже любой из них — чтобы при добавлении нового обязательного шага CI его нельзя
  было забыть, не размножая почти одинаковые проверки (предложено сессией 64).
  → Сделано (сессия 65) проверкой `ci-has-required-steps` стража
  [`structure_guard.py`](../ai-agents/structure_guard.py): единый список
  `REQUIRED_WORKFLOW_COMMANDS` (сейчас `run_all.py --with-tests` и `test_run_all.py`),
  краснеет при пропаже любой команды; две прежние близнецовые проверки удалены,
  число проверок 7→6. `test_structure_guard.py` 49/49, страж зелёный 6/6, `PTD-0062`.
- [x] **Страж структуры: каждой обязательной команде CI — отдельный шаг воркфлоу** —
  развитие `ci-has-required-steps`: проверять не только наличие команды где угодно в
  файле, но что у неё есть собственный шаг `- run:` (а не спрятана в комментарии или в
  одной строке с другой) — чтобы провал одной обязательной команды не маскировал другую
  (предложено сессией 65). → Сделано (сессия 66) проверкой `ci-required-cmd-own-step`
  стража [`structure_guard.py`](../ai-agents/structure_guard.py): помощник
  `_workflow_run_steps` читает тела шагов `run:` (инлайн и блочные), проверка требует,
  чтобы каждая команда из `REQUIRED_WORKFLOW_COMMANDS` запускалась хотя бы одним шагом
  и не делила шаг с другой. Ловит команду, оставшуюся лишь в комментарии (старая
  проверка её «видела»), и две команды в одном `run:`. Число проверок 6→7.
  `test_structure_guard.py` 59/59, страж зелёный 7/7, `PTD-0063`.
- [x] **Страж структуры: у каждого шага `run:` есть человекочитаемое `name:`** —
  мягкое развитие `ci-required-cmd-own-step`: предупреждать (не блокировать), если
  обязательный шаг CI запускается без `- name:` — чтобы в логах GitHub Actions провал
  читался по-человечески («Прогнать всех агентов»), а не голой командой. Опирается на
  уже готовый `_workflow_run_steps`/парсер шагов (предложено сессией 66).
  → Сделано (сессия 67), первая МЯГКАЯ проверка стража `ci-step-has-name`
  (`severity=soft`: `status=warn`, не валит вердикт). Введена общая инфраструктура
  мягких проверок (`_workflow_run_steps_named`, поле `severity`, счётчик `warnings`).
  Число проверок 7→8 (1 мягкая). `test_structure_guard.py` 72/72, страж зелёный 8/8
  без предупреждений, `PTD-0064`.
- [x] **Страж структуры: мягкая проверка `ci-step-name-unique`** — развитие
  `ci-step-has-name` (PTD-0064): предупреждать (не блокировать), если два шага `run:`
  CI делят одинаковое `- name:` — тогда в логах GitHub Actions их не различить, и провал
  читается двусмысленно. Опирается на тот же `_workflow_run_steps_named` (предложено
  сессией 67). → Сделано (сессия 68), вторая МЯГКАЯ проверка `ci-step-name-unique`
  (`severity=soft`: `status=warn`, не валит вердикт): собирает имена всех шагов `run:`,
  предупреждает о повторяющихся, безымянные пропускает. Число проверок 8→9 (2 мягкие).
  `test_structure_guard.py` 80/80, страж зелёный 9/9 без предупреждений, `PTD-0065`.
- [x] **Страж структуры: мягкая проверка «у шага есть и `name:`, и тело `run:`»** —
  развитие `ci-step-name-unique`: предупреждать, если в воркфлоу встречается элемент
  списка с `- name:`, но без `run:`/`uses:` (пустой шаг — частая опечатка отступа),
  чтобы карта шагов CI оставалась честной (предложено сессией 68). → Сделано (сессия 69),
  третья МЯГКАЯ проверка `ci-step-has-body` (`severity=soft`: `status=warn`, не валит
  вердикт): новый помощник `_workflow_step_items` собирает ключи каждого элемента списка
  на «колонке шага»; шаг с `name`, но без `run`/`uses` — предупреждение «пустой шаг».
  Число проверок 9→10 (3 мягкие). `test_structure_guard.py` 89/89 (пустой шаг → warn при
  зелёном вердикте и коде 0; `uses:` считается телом; пути в `paths:` не путаются с
  шагами), страж зелёный 10/10 без предупреждений, `PTD-0066`.
- [ ] **Страж структуры: мягкая проверка «у шага есть `name:`» для ВСЕХ шагов, а не
  только обязательных команд** — сейчас `ci-step-has-name` смотрит лишь шаги
  обязательных команд (`REQUIRED_WORKFLOW_COMMANDS`); шаги `uses:` (checkout, setup-python)
  без имени не предупреждаются. Распространить мягкую проверку имени на все шаги списка
  (на базе `_workflow_step_items`), чтобы вся карта CI читалась по-человечески (предложено
  сессией 69).
- [x] **`run_all`/мета-агент показывает мягкие предупреждения стража** — сейчас
  `warnings` стража печатается только в его собственном отчёте; добавить, чтобы общий
  прогон `run_all --with-tests` тоже выводил суммарное число мягких предупреждений
  (не валя сборку) — чтобы операторская сводка их не теряла (предложено сессией 67).
  → Сделано (сессия 70): мета-агент [`run_all.py`](../ai-agents/run_all.py) теперь
  прогоняет стража структуры заодно (новая `run_guard()`, константа `GUARD`) и
  выводит его отдельной графой в сводке — `страж 10/10, мягких предупреждений
  стража: N` (и в `--json`: ключи `guard`/`guard_warnings`). Hard-«красное» стража
  валит общий вердикт наравне с агентами; мягкие предупреждения только показываются
  числом, не валя сборку; отсутствие стража неприменимо (на вердикт не влияет).
  Тест-инвариант [`test_run_all.py`](../ai-agents/test_run_all.py) 13/13→21/21
  (фейковый страж: предупреждения не валят, hard-red валит, аномалия green+exit≠0
  красна, отсутствие стража неприменимо). `PTD-0067`.
- [x] **`run_all` показывает не только число, но и сами мягкие предупреждения
  стража** — сейчас в сводке выводится суммарное число; добавить под графой стража
  построчный список (какая проверка, какой шаг), чтобы операторская сводка
  объясняла предупреждение без отдельного запуска `structure_guard.py` (развитие
  `PTD-0067`, сессия 70). → Сделано (сессия 71): помощник `guard_warning_lines()` в
  [`run_all.py`](../ai-agents/run_all.py) собирает из `guard.checks` проверки в
  статусе `warn` и их `violations` (item/problem) в строки «[проверка] шаг: проблема»;
  человекочитаемая графа стража при `guard_warnings>0` печатает их списком
  `⚠ [проверка] …` (откат на отсылку к стражу, если детализации нет), а в `--json`
  добавлен ключ `guard_warning_lines`. `test_run_all.py` 21→28, `PTD-0068`.
- [x] **Машиночитаемый «статус-светофор» проекта из `run_all --json`** — сохранять
  последний вердикт `run_all` (включая `guard_warnings` и `guard_warning_lines`) в
  файл-артефакт в `governance/`, как основу будущего публичного статус-индикатора без
  внешних сервисов (родственно давней идее «бейдж в governance/»; теперь, когда строки
  предупреждений машиночитаемы, артефакт может их включать; сессии 70–71).
  → Сделано (сессия 72): флаг `--status-out PATH` + помощник `build_status`,
  детерминированный артефакт [`governance/status/run_all_status.json`](../governance/status/)
  (+ двуязычный README), `test_run_all.py` 28→39, `PTD-0069`. Объединяет давнюю
  идею «бейдж в governance/» (см. ниже) — этот пункт закрыт ею же.
- [x] **Публичный статус-светофор на сайте/странице из артефакта** — `web/`-страница
  (или секция) читает [`governance/status/run_all_status.json`](../governance/status/)
  и рисует человеку зелёно/красно + счёт агентов/тестов и мягкие предупреждения, без
  внешних сервисов/бейджей/трекеров (продолжение `PTD-0069`, сессия 72).
  → Сделано (сессия 73): секция «живой светофор» на странице «Прозрачность»
  ([`web/transparency.html`](../web/transparency.html) +EN) — виджет `.statuslight`
  тем же origin читает `./status.json` и рисует зелёно/красно + счёт агентов/тестов/
  стража и строки мягких предупреждений; общий двуязычный
  [`web/assets/status.js`](../web/assets/status.js) (язык из `<html lang>`, DOM через
  `textContent`, `<noscript>`-фолбэк). Единый источник истины — `governance/status/`:
  деплой Pages ([`pages.yml`](../.github/workflows/pages.yml)) копирует артефакт в
  `web/status.json` на лету (в репо не коммитится, в `.gitignore`) и пере-деплоит сайт
  при смене статуса. Политика «ноль сторонних запросов» сохранена. `PTD-0070`.
- [ ] **Страж/CI: артефакт статуса не отстал от текущего вердикта** — мягкая проверка,
  что зафиксированный `governance/status/run_all_status.json` совпадает с тем, что даёт
  свежий `run_all --status-out` (иначе светофор в репо устарел); либо шаг CI
  пере-генерирует артефакт (детерминизм это позволяет) — `PTD-0069`, сессия 72.
- [x] **Компактный значок-светофор в шапке/футере остальных страниц сайта** — крошечный
  индикатор (зелёная/красная точка + «проверки в порядке»), который тем же `assets/status.js`
  читает `./status.json` и ведёт на секцию «Прозрачность» с подробностями; чтобы статус был
  виден с любой страницы, не только со страницы прозрачности. → Сделано (сессия 74), `PTD-0071`.
- [x] **Значок-светофор также в шапке (topbar), не только в футере** — добавить тот же
  компактный `#statusbadge` в `.nav-tools` шапки стартовых страниц (рядом с переключателем
  языка/темы), чтобы состояние было видно сразу, без прокрутки до футера; на узких экранах —
  аккуратно прятать вместе с навигацией (продолжение `PTD-0071`, сессия 74).
  → Сделано (сессия 75), `PTD-0072`: компактный `#statusbadge-top`
  (`statusbadge statusbadge--compact`) в шапке `index.html` (+EN); общий
  [`status.js`](../web/assets/status.js) теперь рисует все значки `.statusbadge`
  (`querySelectorAll`), один запрос обслуживает и шапку, и футер; на узких экранах
  (`<=640px`) значок шапки прячется (состояние остаётся в футере).
- [ ] **Значок-светофор в шапке/футере страниц `docs`-сайта вне `web/`** — пока светофор
  есть только на стартовых страницах и странице «Прозрачность» (`web/`); распространить тот же
  компактный значок на прочие публичные HTML-страницы, если/когда они появятся, чтобы статус
  был единообразен по всему сайту (продолжение `PTD-0072`, сессия 75).
- [ ] **Перевести Governance-агента на `solidity_scan`** — сейчас он разбирает только
  JSON-конфиги; когда понадобится сверять `Governor.sol`/`Timelock.sol` (вес голоса
  из `Reputation.votingUnits`, а не баланс), переиспользовать общий
  [`solidity_scan.py`](../ai-agents/solidity_scan.py), а не заводить четвёртую копию
  парсинга (продолжение рефактора, сессия 40).
- [x] **CI-проверка «нет копий парсинга `.sol` вне `solidity_scan`»** — мини-линтер,
  который падает, если в `ai-agents/*_agent.py` снова появилось локальное определение
  `strip_solidity_comments`/`function_body` (а не импорт из общего модуля) — чтобы
  устранённое дублирование не вернулось незаметно (родственно «стандарту инвариантов»,
  сессия 40). → Сделано (сессия 51) проверкой `sol-parsing-centralized` стража
  [`structure_guard.py`](../ai-agents/structure_guard.py), `PTD-0048`.
- [ ] **Шаблон/схема записи реестра типа `appeal`** + привязка к Mediator: когда спор
  доходит до `resolved`, итог фиксируется записью `appeal` в `governance/registry/`;
  агент проверяет, что у завершённого спора есть такая запись (замкнуть процесс
  обжалования на публичный реестр, как `disbursement` ↔ escrow; предложено сессией 31).
- [ ] **Контракт распределения по `REWARDS-MODEL.md`** — закодировать параметры §5.1
  (`B_min`/`B_target`/`ρ_min`/`ρ_max`/`w_work`/`rep_cap`/`k`) в контракт над
  [`Treasury.sol`](../contracts/contracts/Treasury.sol)/[`Disbursement.sol`](../contracts/contracts/Disbursement.sol):
  `ρ_cap` и `referral=forbidden` — неизменяемые константы, остальные — `onlyTimelock`
  (через голосование); инвариант-тесты «помощь всегда ≥ 1−ρ_cap», «вес репутации не
  пробивает потолок вилки» (Этап 5, продолжение rewards-model; сессия 41).
- [ ] **Fairness-агент: проверка `rewards-model`** — расширить
  [`fairness_agent.py`](../ai-agents/fairness_agent.py): начисление любого потока
  (H/W/V) пересчитывается по [`REWARDS-MODEL.md`](REWARDS-MODEL.md) и попадает в
  коридоры §2–§3 (доля помощи ≥ 1−ρ_cap; нет реферального потока; репутация —
  множитель, не право на выплату). Замкнёт модель на машинную проверку (сессия 41).
- [ ] **Шаблон/схема записи реестра типа `reward`** (W/V) — по аналогии с
  `disbursement`: проверяемая запись о вознаграждении за работу/волонтёрство
  (артефакт-доказательство, ≥2 проверяющих, сумма в пределах вилки), чтобы каждое
  начисление W/V было в публичном реестре и пересчитываемо (сессия 41).
- [ ] Дашборд казны (read-only) — публичное состояние тестовой казны из реестра.
- [ ] Шаблоны заявок на помощь (анонимные, без перс. данных) — форма + схема.
- [ ] «Объясни как ребёнку» — короткие пояснялки к каждому нормативному доку.
- [ ] Автоматический changelog из реестра решений (генерировать `CHANGELOG.md`).
- [ ] Модель репутации хранителей/проверяющих (анти-сговор) — черновик спецификации.
- [ ] Превратить лендинг-копию из [`PROMOTION.md`](PROMOTION.md) в реальную
  человеко-ориентированную страницу в `web/` (проще нормативного сайта; та же
  политика «без внешних запросов/трекеров»).
- [ ] Медиа-кит проекта: единый логотип/палитра/иконка (SVG, без внешних шрифтов)
  + краткие правила использования — чтобы материалы выглядели согласованно.
- [ ] Пресс-страница/одностраничник «о проекте для прессы и партнёров» (RU/EN) на
  базе boilerplate из `PROMOTION.md` — факты, ссылки, контакты, что можно цитировать.
- [ ] Шаблоны предложений Snapshot (RU/EN) под каждый `proposal_type` из
  `governance/snapshot/space.json` — единый формат (контекст/что предлагается/
  привязка к конституции/варианты голоса), чтобы предложения были сравнимы и проверяемы.
- [x] Единый governance-валидатор в CI: один скрипт прогоняет `registry.py verify`
  + `ipfs_manifest.py verify` + `safe_config.py verify` + `snapshot_config.py verify`
  одной командой (удобный «зелёный/красный» для всего governance-слоя).
  → Реализовано Audit-агентом (сессия 24): [`ai-agents/audit_agent.py`](../ai-agents/audit_agent.py), `PTD-0021`.
- [ ] Тест-инвариант «казна не уходит мимо реестра»: проверять, что у каждого
  on-chain события `Released(registryRef)` есть запись в `governance/registry/`
  (и наоборот) — связать контракт и реестр решений в единую проверку (предложено сессией 19).
- [ ] Скрипт деплоя `contracts/scripts/deploy.js` (Hardhat) с подстановкой адресов
  Safe-мультисига как `executor` — заготовка «до кнопки» для testnet (предложено сессией 19).
- [ ] Тест-инвариант для Audit-агента: подсунуть «сломанный» governance-артефакт
  (битая цепочка/рассинхрон манифеста) и проверить, что агент возвращает «красный» —
  чтобы сам аудит был доказанно рабочим, а не «зелёным по умолчанию» (сессия 24).
- [x] Guardian-агент: явный сканер рельс безопасности по всему репо (нет 64-hex
  ключей/`mnemonic`/`seed`, нет mainnet chain_id, `.env` не в индексе) — переиспользовать
  и обобщить проверки из `safe_config.py`/`snapshot_config.py` (сессия 24).
  → Реализовано (сессия 25): [`guardian_agent.py`](../ai-agents/guardian_agent.py) +
  тест-инвариант [`test_guardian.py`](../ai-agents/test_guardian.py), `PTD-0022`.
- [x] Documentation-агент: линтер двуязычности RU↔EN (пара RU-док ↔ EN-зеркало) +
  проверка целостности относительных ссылок — закрывает и пункт P2 «авто-проверка
  двуязычности в CI» (сессия 24). → Реализовано (сессия 29):
  [`documentation_agent.py`](../ai-agents/documentation_agent.py) +
  [`test_documentation.py`](../ai-agents/test_documentation.py), `PTD-0026`.
- [x] Мета-агент «прогнать всех»: единая точка `ai-agents/run_all.py`, которая
  последовательно вызывает все готовые агенты (Audit + Guardian + …) и сводит их
  отчёты в один «зелёный/красный» для всего проекта (упростит CI и локальный self-check) (сессия 25).
  → Реализовано (сессия 32): [`run_all.py`](../ai-agents/run_all.py) +
  [`test_run_all.py`](../ai-agents/test_run_all.py), `PTD-0029`.
- [ ] Guardian: проверка `pre-commit`-хука/инструкции — заготовка локального хука,
  который гоняет Guardian перед коммитом, чтобы секрет не попал в индекс в принципе (сессия 25).
- [ ] Расширить Guardian на проверку URL-рельс: нет ли в публичных текстах
  «обещаний доходности»/«инвестиция»/«гарантия» (буквальные конституционные запреты
  PRINCIPLES.md) — лёгкий лексический линтер для лендинга/README (сессия 25).
- [ ] Тест-инвариант «выплата ↔ запись реестра» для Fairness/Audit: проверять, что
  у каждой on-chain выплаты есть запись `disbursement` в реестре (и наоборот) —
  связать контракт `Disbursement` и реестр в единую сквозную проверку (сессия 26).
- [ ] Самопроверка агентов в CI как правило: каждый новый агент обязан иметь
  `test_<agent>.py`-инвариант, а CI прогоняет их все — чтобы ни один агент не был
  «зелёным по умолчанию» (стандарт качества Этапа 6, предложено сессией 26).
- [ ] Fairness: проверка согласованности `category` ↔ `priority_level` (например,
  `housing` обычно не ниже уровня «угроза потери жилья») — мягкое предупреждение,
  не блокирующее, чтобы ловить очевидные перекосы категоризации (сессия 26).
- [ ] Reputation-агент: распространить статический разбор «1 человек = 1 голос» на
  `Governor.sol` (вес голоса берётся из `Reputation.votingUnits`, а не из баланса;
  порог внесения предложения равный, не денежный) — замкнуть проверку всей цепочки
  голоса, а не только бейджа (сессия 27).
- [ ] Reputation-агент: сверять `reputationCap` в `scripts/deploy.js`/конфиге деплоя
  с разумным потолком (например, ≤ небольшого N), чтобы множитель за вклад не мог
  тихо превратиться в новую элиту через слишком высокий cap (сессия 27).
- [x] Лёгкий Solidity-инвариант-сканер как переиспользуемый модуль: вырезание
  комментариев + извлечение тела функции по балансу скобок дублировались в
  Reputation/Housing — вынесены в общий
  [`ai-agents/solidity_scan.py`](../ai-agents/solidity_scan.py) (пригодится будущим
  агентам по контрактам Governance/Audit; сессии 27/31). → Сделано (сессия 40),
  `PTD-0037` (та же работа, что закрыла пункт «Рефакторинг общих solidity-помощников»
  выше): тест-инвариант [`test_solidity_scan.py`](../ai-agents/test_solidity_scan.py)
  (14/14), включён в `run_all` (тесты 9/9). Поведение агентов не изменилось.
- [ ] Housing-агент: когда появится вайтлист поставщиков (открытый вопрос
  [`ESCROW-TARGETED-DISBURSEMENT.md`](ESCROW-TARGETED-DISBURSEMENT.md) §8 — кто ведёт
  и как верифицирует арендодателей/аптеки), добавить проверку `provider-whitelisted`:
  `provider` жилищной записи входит в публичный реестр проверенных поставщиков
  (категории `housing`) — закрыть критическую точку доверия модели B (сессия 28).
- [ ] Сквозной тест-инвариант Housing «запись ↔ on-chain escrow»: сверять, что у
  жилищной записи реестра `escrow_id` соответствует реально открытому кейсу в
  `Disbursement` (через события `Opened(id, …, provider, …)`), а `provider` записи =
  `provider` кейса — связать реестр и контракт в одну проверку целевого расхода
  (развитие идеи «выплата ↔ запись реестра», сессия 28).
- [ ] Documentation-агент: проверка «свежести перевода» — RU-док и его EN-зеркало
  должны меняться В ОДНОМ коммите (git-diff: если в коммите/диффе тронут только один
  из пары — мягкое предупреждение). Закроет «двуязычность синхронна», а не только
  «оба файла существуют» (сессия 29).
- [x] Documentation-агент: проверка якорей ссылок (`FILE.md#section`) — что `#section`
  соответствует реальному заголовку в целевом файле, чтобы внутренние ссылки не
  гнили при переименовании разделов (сессия 29). **Сделано (сессия 97):** добавлена
  мягкая проверка `anchor-integrity` — якорь строится по правилу GitHub (нижний
  регистр, пунктуация убрана, пробелы→дефис, кириллица сохраняется, дубли→`-1`/`-2`,
  учтены HTML-якоря `name`/`id`); проверяет и `#section` в том же файле, и кросс-
  файловые `FILE.md#section`; битый ФАЙЛ оставлен блокирующей `link-integrity`.
  Тест-инвариант 14→18 сценариев (38/38), на реальном репо зелёно/0 предупреждений.
  `PTD-0094`.
- [x] Лексический линтер конституционных запретов для публичных текстов
  (README/web/PROMOTION): нет «гарантированной доходности»/«инвестиция»/«пирамида»/
  «рефералы» (буквальные запреты `PRINCIPLES.md`) — может стать частью Documentation
  или отдельным мини-агентом (сессия 29; ранее предлагалось как расширение Guardian).
  **Сделано (сессия 98):** мягкая проверка `constitutional-prohibitions` в
  `documentation_agent.py` — сканирует публичные `.md` + витрину `web/*.html` на
  обещание доходности/прибыли, пассивный доход, зов в инвестицию/пирамиду и плату
  за привлечение (рефералы), RU и EN. Фраза с маркером отрицания/запрета рядом
  (наши дисклеймеры «это НЕ инвестиция», перечни «Запрещается: …», контраст «в
  отличие от …») не считается нарушением; голое обещание — ловится. МЯГКАЯ (warn,
  не роняет вердикт/пульс). Тест-инвариант 18→23 сценария (49/49); на реальном
  репо зелено/0 предупреждений. `PTD-0095`. Возможное расширение — см. «Лог идей».
- [x] Run-All: машиночитаемая сводка-бейдж в `governance/` (последний вердикт
  `run_all --json` сохраняется в файл-артефакт) — основа для будущего публичного
  «статус-светофора» проекта без внешних сервисов (сессия 32).
  → Сделано (сессия 72) тем же флагом `--status-out` и артефактом
  [`governance/status/run_all_status.json`](../governance/status/), `PTD-0069`.
- [x] Тест-инвариант для Audit (`test_audit.py`) — единственный из агентов без
  собственного инварианта; подсунуть «сломанный» governance-артефакт и проверить,
  что Audit краснеет (закрыть пробел стандарта качества Этапа 6; сессия 32).
  → Сделано (сессия 33): [`test_audit.py`](../ai-agents/test_audit.py) (9/9) —
  run_check (pass/fail/error) + сведение main + интеграция с настоящими валидаторами
  на отравленных временных копиях (mainnet chain_id; плутократическая стратегия);
  добавлен в `run_all` (тесты 8/8). `PTD-0030`. **Все 8 агентов теперь с инвариантами.**
- [ ] Общий модуль `ai-agents/agent_report.py` — единый помощник вывода отчёта
  (`{agent, verdict, passed, total, checks}` + человекочитаемый рендер), чтобы
  восемь агентов не дублировали один и тот же код печати и не расходились в формате,
  на который опирается `run_all` (сессия 32; родственно идее `solidity_scan.py`).
- [ ] **Гигиена диска как рельс:** лёгкий скрипт/документ «как агент-строитель
  держит диск чистым» — `contracts/node_modules` (gitignored, ~300 МБ) и кэши не
  должны накапливаться; возможно, шаг `npm ci` только в CI, а локально чистить.
  В сессии 33 диск был забит под 100% (в основном чужими проектами на хосте) —
  стройку это чуть не заблокировало (предложено сессией 33). См. «НУЖНО ОТ ОПЕРАТОРА».
- [x] **Стандарт «все агенты с инвариантами» как явная CI-проверка:** мини-скрипт,
  который падает, если в `ai-agents/` появился `*_agent.py` без парного `test_*.py`
  (и наоборот) — чтобы пробел стандарта качества Этапа 6 (как был у Audit) больше
  не мог возникнуть незаметно (предложено сессией 33; родственно «самопроверке агентов в CI»).
  → Сделано (сессия 51) проверками `agents-have-invariants` + `no-orphan-tests` стража
  [`structure_guard.py`](../ai-agents/structure_guard.py), `PTD-0048`.
- [ ] **Documentation-агент: проверка «жаргон рядом с пояснением»** — лёгкий
  словарный линтер, который для списка технических слов (`proof-of-personhood`,
  `nullifier`, `zero-knowledge`, `liveness`, `escrow`, `multisig`, …) проверяет,
  что в публичном тексте рядом с термином есть человеческое пояснение **или**
  ссылка на [`GLOSSARY.md`](GLOSSARY.md); не блокирует, а предупреждает — чтобы
  правило понятного языка (`PTD-0040`) держалось машинно, а не только на дисциплине
  (предложено сессией 53; родственно идее «мягкая проверка „термин определён“»).
- [x] **Перекрёстные ссылки `IDENTITY-VERIFICATION.md` ↔ `GLOSSARY.md`** — в самом
  документе о проверке личности при первом упоминании каждого технического слова
  поставить ссылку на статью глоссария (и наоборот глоссарий уже ссылается на
  документ) — чтобы непонятный термин был в один клик в обе стороны (предложено
  сессией 53; частный случай идеи «ссылка на глоссарий в шапке доков»).
  → Сделано (сессия 54): шесть терминов (атака Сивиллы, proof-of-personhood,
  liveness, web-of-trust, zero-knowledge, nullifier) при первом упоминании ведут на
  [`GLOSSARY.md`](GLOSSARY.md) + указатель на глоссарий в шапке; RU↔EN, `PTD-0051`.

- [x] **Догнать EN-зеркало `docs/en/ROADMAP.md`** — RU-роадмап ушёл вперёд (раздел
  «Сделано» и банк идей содержат пункты вплоть до `PTD-0051`, EN отставал: P0 Этап 6
  показывал Mediator открытым, в P3-банке не было свежих идентификационных/глоссарных/
  мета-пунктов). Documentation-агент оставался зелёным (пара есть, переключатель,
  ссылки), но содержимое рассинхронизировано — это нарушало правило «RU↔EN синхронно».
  Сделано одним аккуратным проходом (обнаружено сессией 54). → Сделано (сессия 55):
  EN Этап 6 отмечен завершённым (8/8) с модулем Mediator; P3-банк дополнен
  идентификационными/глоссарными/мета-пунктами; в «Сделано» добавлены
  `PTD-0050`/`PTD-0051`/`PTD-0052`. `PTD-0052`.

- [x] **Ссылка на глоссарий в шапке всех нормативных доков** (обобщение) — добавить
  такой же короткий указатель «непонятное слово → [`GLOSSARY.md`](GLOSSARY.md)» в
  шапку остальных публичных доков (`GOVERNANCE`, `ANTI-ABUSE`, `REWARDS-MODEL`,
  `PROOF-OF-CONTRIBUTION`, `ESCROW`, `PRIORITIES`), чтобы глоссарий был в один клик
  из любого документа (предложено сессией 54). → Сделано (сессия 57), `PTD-0054`
  (та же работа, что закрыла пункт «Ссылка на глоссарий в шапке нормативных доков»
  выше): указатель с точным названием группы добавлен во все шесть доков (RU + EN).
- [x] **Указатель на глоссарий в шапке остальных публичных доков** — распространить
  тот же навигационный указатель на нетехнические, но всё ещё содержащие термины
  доки (`SUPPORT-MODEL`, `ACCOUNTABILITY`, `SECURITY`, `PRINCIPLES`, `CONSTITUTION`),
  чтобы правило «термин в один клик от объяснения» действовало единообразно во всём
  корпусе документов (предложено сессией 57; продолжение P3 «ссылка на глоссарий»).
  → Сделано (сессия 59), `PTD-0056`: указатель с точной группой глоссария добавлен во
  все пять доков (RU + EN). Правило понятного языка теперь действует единообразно во
  всём корпусе публичных документов.

- [ ] **Documentation-агент: мягкая проверка «термин в шапке доков ведёт в глоссарий»** —
  лёгкая read-only проверка, что у публичных доков, содержащих известные технические
  слова, в шапке есть указатель на `GLOSSARY.md` с группой, чьё название совпадает с
  реальным заголовком в глоссарии; предупреждает (не блокирует), чтобы навигационная
  связь не отставала от новых доков (предложено сессией 59; родственно идее «жаргон
  рядом с пояснением»).

- [ ] **Documentation-агент: предложить ближайший заголовок при битом якоре** —
  развитие `anchor-integrity` (`PTD-0094`): когда якорь не найден, не просто
  предупреждать, а подсказывать самый похожий существующий слаг целевого файла
  (по расстоянию Левенштейна/общей подстроке), чтобы человеку было сразу видно, на
  что переименовали раздел. Останется мягкой подсказкой, не блоком (предложено сессией 97).

---

## Сделано

- **PTD-0088 (сессия 91):** INBOX #34 (платформа) — **экран «Кому фонд помогает раньше: порядок
  помощи»** ([`platform/app/priorities/`](../platform/app/priorities/)) — зеркало нормативного
  [`PRIORITIES.md`](PRIORITIES.md) внутри приложения. Простым языком (PTD-0040): когда денег в
  казне меньше, чем просьб, чью беду закрывают первой. Ведущий раздел «Главное — простыми словами»
  (5 опор), нумерованный порядок 10 уровней (короткие подписи переиспользованы из
  `apply.priorityLabels`, человеческие пояснения — `priorities.meanings`), «Как это работает на
  деле», честная плашка «срочность не отменяет проверку», ссылка на экран заявки и на нормативный
  документ. Переиспользует `PRIORITIES` из [`lib/application.ts`](../platform/lib/application.ts)
  (ничего не задвоено), без новых зависимостей (ноль исходящих запросов). Двуязычно RU↔EN
  (`PrioritiesDict`), добавлен в постоянное меню и связан ссылкой из экрана «Подать заявку».
- **PTD-0085 (сессия 88):** INBOX #34 (платформа) — **экран «Что приложение помнит о вас»**
  ([`platform/app/privacy/`](../platform/app/privacy/)) + слой данных
  [`platform/lib/storage.ts`](../platform/lib/storage.ts). Единое место, где человек видит ВСЁ,
  что платформа сохранила в его браузере (выбор на «Проверке личности», голоса-черновики), и может
  стереть это целиком или по одной записи. Право «видеть и удалить свои данные» (достоинство, без
  слежки — [`ACCOUNTABILITY.md`](ACCOUNTABILITY.md)/[`GOVERNANCE.md`](GOVERNANCE.md)). Все записи
  лежат под общей приставкой `ptd:` — новые экраны попадают в список автоматически; кошелёк
  намеренно не хранится (читается вживую). Ничего не уходит наружу и не попадает в блокчейн.
  Двуязычно RU↔EN (`MyDataDict`), понятный язык (PTD-0040), без новых зависимостей; добавлен в
  постоянное меню и подвал.
- **PTD-0072 (сессия 75):** P2 (прозрачность) — **значок-светофор также в шапке (topbar),
  не только в футере.** В шапку стартовых страниц ([`web/index.html`](../web/index.html) + EN),
  в `.nav-tools` рядом с переключателем языка/темы, добавлен компактный значок `#statusbadge-top`
  (`statusbadge statusbadge--compact`) — состояние видно сразу при загрузке, без прокрутки до
  футера. Общий [`web/assets/status.js`](../web/assets/status.js) отрефакторен: рисует ВСЕ
  значки `.statusbadge` на странице (`querySelectorAll` вместо одиночного `getElementById`),
  один запрос `./status.json` обслуживает и шапочный, и футерный значок. На узких экранах
  (`<=640px`) значок шапки скрыт вместе с навигацией — состояние там остаётся видно в футере.
  Политика «ноль сторонних запросов» сохранена. Продолжение `PTD-0071`.
- **PTD-0071 (сессия 74):** P2 (прозрачность) — **компактный значок-светофор на остальных
  страницах сайта.** В футер стартовых страниц ([`web/index.html`](../web/index.html) + EN)
  добавлен компактный значок `#statusbadge`: маленькая точка + слово («проверки в порядке» /
  «есть расхождение»), который тем же origin читает `./status.json` и ведёт на секцию
  «Прозрачность» (`#livestatus`) с подробностями — статус виден с любой страницы, а не только
  со страницы прозрачности. Общий [`web/assets/status.js`](../web/assets/status.js)
  отрефакторен: один запрос рисует и полный виджет `#statuslight`, и компактный значок
  `#statusbadge` (тот, что есть на странице). Стиль `.statusbadge` — на токенах темы, без
  внешних зависимостей. Политика «ноль сторонних запросов» сохранена. Продолжение `PTD-0070`.
- **PTD-0070 (сессия 73):** P2 (прозрачность) — **публичный «живой светофор» состояния
  проекта на сайте.** На странице «Прозрачность» ([`web/transparency.html`](../web/transparency.html)
  + EN) добавлена секция `#livestatus` с виджетом `.statuslight`: общий двуязычный
  [`web/assets/status.js`](../web/assets/status.js) тем же origin читает `./status.json` и
  рисует человеку зелёно/красно + счёт агентов/тестов/стража и строки мягких предупреждений
  (язык из `<html lang>`, DOM через `textContent`, `<noscript>`-фолбэк на сам артефакт и CI).
  Делает работу сессий 70–72 видимой человеку. Единый источник истины — `governance/status/`:
  деплой Pages ([`pages.yml`](../.github/workflows/pages.yml)) копирует артефакт в `web/status.json`
  на лету (в репо не коммитится, добавлен в `.gitignore`) и пере-деплоит сайт при смене статуса.
  Сохранена политика «ноль сторонних запросов/бейджей/трекеров» — запрос только same-origin.
  Служит ст. 3 (понятность) и ст. 9 (ИИ — сигнал, не власть). verify=зелёный (71 запись),
  run_all 8/8 тесты 11/11 страж 10/10, documentation 3/3, `node --check status.js` OK.
- **PTD-0069 (сессия 72):** P3 (качество Этапа 6) — **машиночитаемый «статус-светофор»
  проекта из `run_all`.** Сводит накопленную за сессии 70–71 работу (`guard_warnings` и
  `guard_warning_lines` в `--json`) в видимый операторский/публичный артефакт, а не очередной
  невидимый guard-микрочек. Флаг `--status-out PATH` + помощник `build_status(report)`:
  компактная **детерминированная** сводка (общий вердикт, счёт агентов/тестов, сводка стража
  с числом и строками мягких предупреждений, по строке на каждого агента). Без времени по
  стенным часам → в git «грязнеет» только при смене вердикта/счёта; `schema_version`
  версионирует формат; пишется при любом вердикте (в т.ч. `red`). Канонический артефакт
  [`governance/status/run_all_status.json`](../governance/status/) + двуязычный README.
  Закрывает обе родственные идеи (этот пункт и давний «бейдж в `governance/`», сессия 32).
  `test_run_all.py` 28→39 (11 новых проверок: файл не пишется без флага, пишется и валиден,
  схема версионирована, счёт сведён, детерминизм — повторный прогон побайтово тот же файл,
  красный прогон пишет `verdict=red`, `build_status` терпит `guard=None`). verify=зелёный
  (70 записей), run_all 8/8 тесты 11/11 страж 10/10, documentation 3/3.
- **PTD-0068 (сессия 71):** P3 (качество Этапа 6) — **`run_all` показывает не только
  ЧИСЛО, но и сами СТРОКИ мягких предупреждений стража структуры.** Развитие PTD-0067
  (сессия 70): новый помощник `guard_warning_lines(guard_result)` собирает из `guard.checks`
  все проверки в статусе `warn` (severity=soft) и их `violations` (item/problem) в список
  строк «[ключ-проверки] шаг: проблема». Человекочитаемая графа стража при
  `guard_warnings>0` печатает их построчно `⚠ [проверка] …` вместо прежней отсылки
  «подробности — `python3 ai-agents/structure_guard.py`» (отсылка осталась страховкой, если
  `warnings>0`, но детализации в `checks` нет). В `--json` добавлен ключ `guard_warning_lines`
  — основа будущего машиночитаемого статус-светофора. Hard-«красное» стража по-прежнему валит
  общий вердикт, мягкие — нет. `test_run_all.py` 21→28 (fake_guard расширен параметром `checks`;
  7 новых проверок: вывод item/problem/ключа, только warn собираются, откат без деталей).
  verify=зелёный (69 записей), IPFS=OK (19), run_all 8/8 тесты 11/11 страж 10/10.
- **PTD-0066 (сессия 69):** P3 (качество Этапа 6) — **страж структуры: десятая (третья
  МЯГКАЯ) проверка `ci-step-has-body` (у шага с `- name:` есть тело `run:`/`uses:` — он не
  пустой).** Развитие `ci-step-name-unique` (PTD-0065) в сторону честности карты шагов CI:
  если элемент списка несёт `- name:`, но НИ `run:`, НИ `uses:`, это почти всегда опечатка
  отступа — тело шага «выпало» из него, шаг ничего не делает, хотя в интерфейсе GitHub
  Actions выглядит настоящим. Новый помощник `_workflow_step_items` собирает ключи каждого
  элемента списка на «колонке шага» (отступ `-` + 2), бесключевые элементы `paths:` не
  мешают. Не ломает покрытие (реальный пропуск команды ловят hard-проверки) → лишь
  **предупреждает** (`severity=soft`, `status=warn`), вердикт остаётся зелёным. Число
  проверок 9→10 (3 мягкие). `test_structure_guard.py` 80→89 (3 новых блока: пустой шаг →
  warn при зелёном вердикте и коде 0; `uses:` считается телом; пути в `paths:` не путаются
  с шагами), страж 10/10 без предупреждений, run_all 8/8 + тесты 11/11, IPFS verify=OK (19),
  реестр 67. README агента (RU/EN): новая строка таблицы с пометкой «мягкая».
- **PTD-0065 (сессия 68):** P3 (качество Этапа 6) — **страж структуры: девятая (вторая
  МЯГКАЯ) проверка `ci-step-name-unique` (два шага `run:` CI не делят одинаковое `- name:`).**
  Развитие `ci-step-has-name` (PTD-0064): имя у шага уже есть, но если оно повторяется,
  упавший шаг в логах GitHub Actions читается двусмысленно («какой именно из двух
  одноимённых?»). Проверка собирает имена всех шагов `run:` (готовый помощник
  `_workflow_run_steps_named`) и предупреждает о повторяющихся; безымянные пропускает (о них
  заботится `ci-step-has-name`). Не ломает покрытие → лишь **предупреждает** (`severity=soft`,
  `status=warn`), вердикт остаётся зелёным. Число проверок 8→9 (2 мягкие).
  `test_structure_guard.py` 72→80 (3 новых сценария: два шага с одним именем → warn при
  зелёном вердикте и коде 0; безымянные не краснят; разные имена в раздельных блоках →
  зелёная), страж 9/9 без предупреждений, run_all 8/8 + тесты 11/11, IPFS verify=OK (19),
  реестр 66. README агента (RU/EN): новая строка таблицы с пометкой «мягкая».
- **PTD-0064 (сессия 67):** P3 (качество Этапа 6) — **страж структуры: восьмая (первая
  МЯГКАЯ) проверка `ci-step-has-name` (у шага `run:` обязательной команды CI есть
  человеческое `- name:`).** Развитие `ci-required-cmd-own-step` (PTD-0063) в сторону
  читаемости: без имени упавший шаг показан в логах GitHub Actions голой командой
  (`python3 ai-agents/run_all.py --with-tests`), а не по-человечески («Прогнать всех
  агентов»). Не ломает покрытие → проверка лишь **предупреждает** (`severity=soft`,
  `status=warn`), вердикт остаётся зелёным. Введена общая инфраструктура мягких проверок:
  помощник `_workflow_run_steps_named` (тела шагов `run:` с их именами; прежний
  `_workflow_run_steps` стал обёрткой), поле `severity` в `CHECKS`, счётчик `warnings` в
  отчёте. Число проверок 7→8 (1 мягкая). `test_structure_guard.py` 59→72 (3 новых сценария:
  оба шага без имени → warn при зелёном вердикте; смешанный; блочные шаги с именами), страж
  8/8 без предупреждений, run_all 8/8 + тесты 11/11, IPFS verify=OK (19), реестр 65.
  README агента (RU/EN): новая строка таблицы с пометкой «мягкая».
- **PTD-0063 (сессия 66):** P3 (качество Этапа 6) — **страж структуры: седьмая проверка
  `ci-required-cmd-own-step` (у каждой обязательной команды CI свой шаг `run:`).** Развитие
  `ci-has-required-steps` (PTD-0062): прежняя проверка подтверждала, что команда есть
  *где-то* в файле — включая комментарии и названия шагов, а две команды могли делить один
  шаг `run:`. Новая проверка читает сами тела шагов `run:` (новый помощник
  `_workflow_run_steps` парсит инлайн- и блочные `run:`) и требует, чтобы каждая команда из
  `REQUIRED_WORKFLOW_COMMANDS` (а) реально запускалась хотя бы одним шагом и (б) не делила
  шаг с другой — иначе провал одной маскирует другую (`&&` обрывает цепочку; команда в
  комментарии не исполняется). Число проверок 6→7. `test_structure_guard.py` 49→59 (4 новых
  сценария, включая команду-лишь-в-комментарии и две-команды-в-одном-шаге), страж 7/7,
  run_all 8/8 + тесты 11/11, IPFS verify=OK (19), реестр 64. README агента (RU/EN): новая
  строка таблицы.
- **PTD-0062 (сессия 65):** P3 (качество Этапа 6) — **страж структуры: проверка
  `ci-has-required-steps` (единый список обязательных команд CI).** Обобщил две прежние
  близнецовые проверки `ci-calls-run-all` (PTD-0057) и `ci-runs-test-run-all` (PTD-0061)
  в одну, основанную на декларативном списке `REQUIRED_WORKFLOW_COMMANDS` (ключ, regex,
  «зачем»): страж краснеет, если воркфлоу пропустил любую обязательную команду (сейчас
  `run_all.py --with-tests` и `test_run_all.py`). Теперь новый обязательный шаг CI — одна
  строка в списке, без размножения почти одинакового кода проверок. Число проверок стража
  7→6 (консолидация). `test_structure_guard.py` 49/49 (сценарии мигрированы; добавлено,
  что нарушение названо ровно пропавшей командой), страж 6/6, run_all 8/8 + тесты 11/11,
  IPFS verify=OK (19), реестр 63. README агента (RU/EN): строка таблицы заменена,
  счётчик 50/50 → 49/49.
- **PTD-0061 (сессия 64):** P3 (качество Этапа 6) — **страж структуры: седьмая проверка
  `ci-runs-test-run-all`.** CI-воркфлоу обязан отдельным шагом прогонять тест-инвариант
  мета-агента `test_run_all.py`, а не только `run_all.py --with-tests`: сам `run_all`
  СВОДИТ частные «красное» в общий вердикт, и эта сводка могла бы тихо сломаться
  (рапортовать «зелено» при красном агенте) — исправность доказывает именно
  `test_run_all.py`. Без отдельного шага поломку сведения никто бы не поймал. Достроил
  защиту покрытия от обхода до самого верха: списки `AGENTS`/`TESTS` (PTD-0049) → вызов
  `run_all` в CI (PTD-0057) → срабатывание триггера (PTD-0060) → прогон тест-инварианта
  сведения (PTD-0061). `test_structure_guard.py` 50/50, страж 7/7, run_all 8/8 + тесты
  11/11, IPFS verify=OK (19), реестр 62. README агента (RU/EN): новая строка таблицы,
  счётчик 43/43 → 50/50.

- **PTD-0060 (сессия 63):** P3 (качество Этапа 6) — **страж структуры: шестая проверка
  `trigger-paths-include-agents`.** Блоки триггеров CI-воркфлоу `on.push.paths` и
  `on.pull_request.paths` обязаны содержать `ai-agents/**` — иначе правка агента не
  запустит CI и покрытие обходится ещё раньше, на уровне триггера, чем на уровне команды
  (`ci-calls-run-all` сторожит вызов мета-агента, но вызов бесполезен, если воркфлоу не
  срабатывает). Лёгкий парсер YAML по отступам, без зависимостей. Замкнул защиту покрытия
  от обхода на всех уровнях: списки `AGENTS`/`TESTS` (PTD-0049) → вызов `run_all` в CI
  (PTD-0057) → срабатывание триггера (PTD-0060). `test_structure_guard.py` 43/43, страж
  6/6, run_all 8/8 + тесты 11/11, IPFS verify=OK (19), реестр 61. Заодно догнаны README
  агента (RU/EN): отставшая строка `ci-calls-run-all` + новая строка, счётчик 25/25 → 43/43.

- **PTD-0056 (сессия 59):** P3 (понятный язык) — **указатель на глоссарий в шапке
  остальных публичных доков.** В шапку `SUPPORT-MODEL` / `ACCOUNTABILITY` / `SECURITY` /
  `PRINCIPLES` / `CONSTITUTION` (RU + EN) добавлена строка «Непонятные технические слова
  … объяснены простым языком в [`GLOSSARY.md`](GLOSSARY.md) — группа «…»» с точным
  названием группы (Деньги и помощь / Прозрачность и проверяемость / Безопасность и
  техника / Основы). Для `SECURITY.md` (в корне) ссылка ведёт на `docs/GLOSSARY.md` /
  `docs/en/GLOSSARY.md`. Завершает обобщение PTD-0054: правило «термин в один клик от
  объяснения» теперь действует единообразно во всём корпусе публичных документов. Смысл
  доков не изменён. Documentation 3/3, run_all 8/8 + тесты 11/11, IPFS verify=OK (19),
  реестр 57.

- **PTD-0054 (сессия 57):** P3 (понятный язык) — **указатель на глоссарий в шапке
  шести нормативных доков.** В шапку `GOVERNANCE` / `ANTI-ABUSE` / `REWARDS-MODEL` /
  `PROOF-OF-CONTRIBUTION` / `ESCROW-TARGETED-DISBURSEMENT` / `PRIORITIES` (RU + EN)
  добавлена строка «Непонятные технические слова … объяснены простым языком в
  [`GLOSSARY.md`](GLOSSARY.md) — группа «…»» с точным названием группы (Управление и
  голосование / Безопасность и техника / Деньги и помощь). Непонятный термин теперь в
  один клик от человеческого объяснения; связь с глоссарием двусторонняя. Смысл доков
  не изменён. Documentation 3/3, run_all 8/8 + тесты 11/11, IPFS verify=OK (19),
  реестр 55. Закрыл два пункта P3-банка (базовый + обобщение).

- **PTD-0053 (сессия 56):** INBOX #22 (запрос оператора) — **«Защита на будущее
  (в т.ч. от квантовых компьютеров)» в [`SECURITY.md`](../SECURITY.md) (+ EN).**
  Простым языком (правило `PTD-0040`) + техраздел «для разработчиков». Позиция: НЕ
  внедрять PQC в смарт-контракты сейчас, а заложить фундамент — **сменность алгоритмов
  (crypto-agility)**, только факты/отпечатки on-chain (обезвреживает
  «harvest-now-decrypt-later»), ключи у человека, готовность к NIST PQC (ML-KEM/Kyber,
  ML-DSA/Dilithium, SPHINCS+), идти за Ethereum/Polygon. Отдельный подраздел **«Защита
  денег фонда (казны)»** по уточнению оператора: мультисиг 3-из-5, «переезд» казны на
  квантово-стойкий кошелёк (главная закладка), смена/отзыв ключей хранителей без потери
  средств, холодный резерв; честно о приоритете — защита денег сейчас = аудит +
  мультисиг + гигиена ключей, квантовость — слой «на будущее» без задержки запуска.
  Зарегистрировано в реестре (54 записи, verify=зелёный), IPFS verify=OK, Documentation
  3/3, run_all 8/8 + тесты 11/11. TESTNET-first.

- **PTD-0052 (сессия 55):** P3 (понятный язык / синхронизация RU↔EN) — **догнан
  EN-зеркало [`docs/en/ROADMAP.md`](en/ROADMAP.md)**. RU-роадмап ушёл вперёд, EN
  отставал по содержанию: P0 Этап 6 показывал модуль Mediator открытым (в RU он 8/8
  завершён), а P3-банк идей не содержал свежих пунктов (`PersonhoodVerifier`; термины
  проверки личности в глоссарии, `PTD-0050`; перекрёстные ссылки IDENTITY↔GLOSSARY,
  `PTD-0051`; запись реестра типа `appeal`; обобщение «ссылка на глоссарий в шапке
  доков»). Documentation-агент оставался зелёным (пара/переключатель/ссылки целы), но
  содержимое было рассинхронено — нарушение правила «RU↔EN синхронно». Одним проходом:
  Этап 6 отмечен завершённым с модулем Mediator (`PTD-0028`); P3-банк синхронизирован;
  в «Сделано» добавлены `PTD-0050`/`PTD-0051`/`PTD-0052`. Денег/ключей/контрактов не
  касается. `PTD-0052`. TESTNET-ONLY. Закрывает идею сессии 54.

- **PTD-0051 (сессия 54):** P3 (понятный язык) — **перекрёстные ссылки
  `IDENTITY-VERIFICATION.md` ↔ `GLOSSARY.md`**. В документе о проверке личности
  (+EN) при первом упоминании каждого технического слова поставлена ссылка на
  статью глоссария — покрыты все шесть терминов группы «Проверка личности» (атака
  Сивиллы, proof-of-personhood, liveness, web-of-trust, zero-knowledge, nullifier);
  в шапку добавлен указатель «непонятные слова → [`GLOSSARY.md`](GLOSSARY.md), группа
  „Проверка личности“». Теперь связь двусторонняя: глоссарий ведёт на документ
  (PTD-0050), документ — на глоссарий. Documentation-агент 3/3 (link-integrity
  зелёный); IPFS verify=OK (19); run_all --with-tests: агенты 8/8, тесты 11/11.
  `PTD-0051`. TESTNET-ONLY. Закрывает идею сессии 53.

- **PTD-0050 (сессия 53):** P3 (банк идей, понятный язык) — **термины проверки
  личности добавлены в глоссарий простыми словами**. Новая группа «Проверка
  личности — что за аккаунтом живой человек» в [`GLOSSARY.md`](GLOSSARY.md) (+EN):
  шесть статей человеческим языком — «защита от накруток и фейковых аккаунтов»
  (атака Сивиллы), «подтверждение, что ты живой уникальный человек»
  (proof-of-personhood), «проверка живости» (liveness), «доказательство без
  раскрытия» (zero-knowledge), «одноразовая пометка „уже зарегистрирован“»
  (nullifier), «поручительство живых людей» (web-of-trust, запасной путь без
  камеры). Каждая статья ведёт на [`IDENTITY-VERIFICATION.md`](IDENTITY-VERIFICATION.md);
  технический термин — в скобках для разработчиков (правило понятного языка,
  `PTD-0040`). Список групп в «Как пользоваться» дополнен; RU↔EN синхронно.
  Documentation-агент: 3/3 (пары/переключатель/ссылки зелёные); IPFS-манифест
  пересобран (verify=OK). `PTD-0050`. Закрывает идею сессии 44.

- **PTD-0049 (сессия 52):** P3 (стандарты качества Этапа 6) — **страж структуры
  расширен проверкой `run-all-covers-all`** в
  [`structure_guard.py`](../ai-agents/structure_guard.py) (тест-инвариант 17→25).
  Прямое продолжение PTD-0048: четвёртый структурный стандарт — каждый `*_agent.py`
  обязан быть в списке `AGENTS` мета-агента [`run_all.py`](../ai-agents/run_all.py),
  а каждый `test_*.py` — в списке `TESTS` (и наоборот: нет висячих ссылок на
  исчезнувшие файлы). Так новый агент/тест нельзя завести в **обход общего прогона
  CI** (`run_all --with-tests`) — иначе он тихо не проверялся бы. Проверка разбирает
  `run_all.py` статически (текстом, не исполняя). **Заодно закрыт реальный пробел:**
  `test_run_all.py` существовал, но НЕ был в `TESTS` — мета-агент не прогонял
  собственный тест-инвариант; теперь добавлен (тесты 10→11). README RU/EN и CI-шаг
  обновлены. На реальном репо страж 4/4 зелёный; `run_all --with-tests`: агенты 8/8,
  тесты 11/11. `PTD-0049`. TESTNET-ONLY.

- **PTD-0048 (сессия 51):** P3 (стандарты качества Этапа 6) — **страж структуры**
  [`ai-agents/structure_guard.py`](../ai-agents/structure_guard.py) (+ тест-инвариант
  [`test_structure_guard.py`](../ai-agents/test_structure_guard.py), 17/17). Закрывает
  сразу две открытые идеи роадмапа. Служебный read-only модуль (ст. 9) смотрит на сам
  каталог `ai-agents/` и стережёт три структурных стандарта: `agents-have-invariants`
  (у каждого `*_agent.py` есть парный `test_<имя>.py` — агент не «зелёный по умолчанию»),
  `no-orphan-tests` (у каждого `test_*.py` есть исходный модуль), `sol-parsing-centralized`
  (ни один `*_agent.py` не заводит локальную копию помощников разбора `.sol` — их
  импортируют из общего [`solidity_scan.py`](../ai-agents/solidity_scan.py)). Включён в
  `run_all --with-tests` (тесты 9→10), CI краснеет при регрессе. README RU/EN обновлены.
  На реальном репо страж 3/3 зелёный; `run_all --with-tests`: агенты 8/8, тесты 10/10.
  `PTD-0048`. TESTNET-ONLY.

- **PTD-0039 (сессия 42):** INBOX #18 (оператор) — **[`PROOF-OF-CONTRIBUTION.md`](PROOF-OF-CONTRIBUTION.md) (+EN)**,
  доказательство вклада и эскроу-контракты. Фонд создаёт оплачиваемую работу ЧЕРЕЗ
  помощь людям; центральная проблема — доказательство выполнения (**проблема оракула**).
  Архитектура: задача с критериями приёмки (bounty/milestone) + эскроу блокирует награду
  при назначении; доказательство тремя слоями (подпись получателя помощи + артефакты-хеши
  в IPFS, файл приватный + автопроверка ончейн-работы merge/тесты); валидация коллективная
  (≥N независимых, исполнитель≠валидатор) + Fairness/Audit AI (детект аномалий) +
  **валидаторы СТАВЯТ репутацию** (пропустил фрод → теряешь) + окно споров/медиация;
  `release` только при подтверждение∧≥N валидаторов∧истёкшее окно, всё в реестре+on-chain;
  анти-фрод (анти-Сивилла против сговора «волонтёр=нуждающийся», лимиты, рандомные
  перепроверки, нужда приоритетнее награды). Честно о слабом месте оракула: старт с малых
  сумм, масштаб по доверию. Эскиз `IContributionEscrow` под Этап 5. Documentation/Guardian/
  Audit зелёные, IPFS пересобран (17). `PTD-0039`. TESTNET-ONLY.
- **PTD-0038 (сессия 41):** INBOX #17 (оператор) — **[`REWARDS-MODEL.md`](REWARDS-MODEL.md) (+EN)**,
  базовая **адаптивная параметрическая** модель вознаграждения и распределения.
  (1) Адаптивность: распределяемый бюджет `D = balance − буфер B_min`; индекс
  здоровья казны `h∈[0,1]`; доля на вознаграждение `ρ = ρ_min+(ρ_max−ρ_min)·h`
  растёт с `h`, но под жёстким конституционным потолком `ρ_cap` (по умолчанию 0.30 →
  помощь всегда ≥70 %, в дефиците почти всё на нужду); коэффициенты под голосованием
  (`parameter-change`→Timelock), `ρ_cap`/`referral=forbidden`/буфер — `governed:false`.
  (2) Три раздельных потока (диапазоны, не суммы): H помощь нуждающимся (целевой
  расход поставщику по [`PRIORITIES.md`](PRIORITIES.md)), W работа/вклад (вилки по
  сложности, репутация — только множитель к позиции в вилке), V волонтёрство
  (признание/неденежно в дефиците); реферального потока нет (ст. 6.2). (3) Валидация:
  ≥2 независимых проверяющих (никто не одобряет себя), анти-Сивилла через
  soulbound-бейдж, Fairness AI пересчитывает + Audit AI сверяет с реестром/on-chain,
  каждое начисление — запись реестра + событие, у каждого отказа апелляция.
  Машиночитаемые параметры §5.1 — спецификация под Этап 5 (контракт распределения над
  Treasury/Disbursement) и Fairness/Audit AI. Documentation/Guardian/Audit зелёные,
  IPFS пересобран. `PTD-0038`. TESTNET-ONLY.
- **PTD-0037 (сессия 40):** P3 (банк идей) — **рефакторинг общих Solidity-помощников
  AI-агентов** в единый модуль [`ai-agents/solidity_scan.py`](../ai-agents/solidity_scan.py).
  Лёгкий текстовый разбор `.sol` (`strip_solidity_comments`/`has_function`/
  `function_signature`/`function_body`) дублировался в контрактных агентах
  (Reputation, Housing) — копии могли тихо разойтись. Вынесен в один read-only модуль
  без зависимостей (ст. 9 — служебный, ничего не двигает); оба агента импортируют его.
  Добавлен тест-инвариант [`test_solidity_scan.py`](../ai-agents/test_solidity_scan.py)
  (14/14): комментарии вырезаются, функция ищется по ИМЕНИ а не подстроке, тело
  извлекается по балансу скобок (а не до первой `}`), сигнатура парсится. Включён в
  `run_all` (тесты 9/9) и CI `ai-agents.yml` автоматически (`run_all --with-tests`).
  **Поведение агентов не изменилось:** Reputation 5/5, Housing 8/8; их инварианты
  17/17 и 23/23 как до рефакторинга. `PTD-0037`. TESTNET-ONLY.
- **PTD-0036 (сессия 39):** P3 (качество/прозрачность) — **`SECURITY.md` (+EN)**,
  каноническая политика безопасности (GitHub распознаёт, кнопка «Report a
  vulnerability» / вкладка Security). Что считается уязвимостью (контракты/рельсы/
  утечка секрета/подрыв проверяемости/сайт/CI), что НЕ по этому каналу, приватное
  сообщение (Security Advisories / `security@` после настройки почты / форма «рельс»),
  чего ждать (coordinated disclosure, без денежной награды — общественное благо),
  **safe harbor** для добросовестного исследования, конституционные рельсы и область
  действия. Кросс-линки: CODE_OF_CONDUCT (+EN), README (+EN), REPO-STRUCTURE (+EN).
  Documentation-агент зелёный (52 публичных дока, пары RU↔EN, 0 битых ссылок).
  TESTNET-ONLY, без реальных средств/ключей.
- **PTD-0035 (сессия 38):** P2 (качество/прозрачность) — **страница «Прозрачность —
  проверь сам»** на сайте: [`web/transparency.html`](../web/transparency.html) (+EN
  зеркало `transparency.en.html`). В одном месте собраны ссылки на проверяемые
  артефакты (публичный реестр решений, IPFS-манифест, статусы CI на GitHub Actions,
  смарт-контракты и тесты, AI-агенты) и даны пошаговые команды самостоятельной
  проверки целостности (`git clone`; `scripts/registry.py verify`;
  `scripts/ipfs_manifest.py verify`; `ai-agents/audit_agent.py [--with-contracts]`;
  сверка с публичным CI). Показан текущий `head_hash` реестра как итоговый отпечаток
  всей цепочки. **Рельс «ноль исходящих запросов» соблюдён:** статусы CI даны
  текстовыми ссылками, а НЕ картинками-бейджами со стороннего сервера. Ссылки на
  страницу добавлены в секцию «Прозрачность» и футер обоих `index`-файлов.
  Documentation-агент и Audit зелёные. TESTNET-ONLY.
- **PTD-0034 (сессия 37):** P2 (качество/прозрачность) — **шаблоны issue/PR**.
  `.github/ISSUE_TEMPLATE/` (`config.yml` + 4 YAML-формы: ошибка/неточность,
  идея, governance-предложение, риск злоупотребления/дыра в рельсах) +
  двуязычный `.github/pull_request_template.md`. Все формы двуязычны (RU+EN в
  одном теле) и содержат чек-листы конституционных рельс (общественное благо
  НЕ инвестиция; TESTNET-first; секреты не в репо; двуязычность RU↔EN; тесты до
  зелёного; прозрачность через реестр). Формы issue сделаны как YAML
  (структурированы, валидируют поля, не попадают в .md-скан двуязычности);
  шаблон PR — markdown (требование GitHub), двуязычный внутри себя, добавлен в
  `SINGLE_LANG` Documentation-агента. `config.yml` выключает пустые issue и даёт
  contact-ссылки на CONTRIBUTING/CODE_OF_CONDUCT/ROADMAP; форма безопасности
  просит не публиковать боевые шаги (приватный канал — после настройки почты).
  Упомянуто в CONTRIBUTING (RU/EN). Documentation-агент зелёный, `run_all
  --with-tests`: агенты 8/8, тесты 8/8. `PTD-0034`. TESTNET-ONLY.
- **PTD-0033 (сессия 36):** P2 (качество/прозрачность) — **глоссарий**.
  [`GLOSSARY.md`](GLOSSARY.md) (+ EN [`docs/en/GLOSSARY.md`](en/GLOSSARY.md)) —
  ~40 ключевых терминов проекта **простым языком** для не-технических читателей,
  в 5 смысловых группах (основы; управление и голосование; деньги и помощь;
  прозрачность и проверяемость; безопасность и техника). Каждый термин — короткое
  «как для человека без бэкграунда» определение + ссылка на нормативный документ,
  где это закреплено (CONSTITUTION/GOVERNANCE/PRIORITIES/ANTI-ABUSE/контракты).
  Закрывает разрыв «доступности» миссии: документы понятны не только техническим.
  Внесён в карту доков обоих README; Documentation-агент зелёный (пара RU↔EN /
  переключатель / 0 битых ссылок). `PTD-0033`. TESTNET-ONLY.
- **PTD-0031 (сессия 34):** P2 (качество/прозрачность) — **CONTRIBUTING.md (+EN)**.
  [`CONTRIBUTING.md`](../CONTRIBUTING.md) + [`CONTRIBUTING.en.md`](../CONTRIBUTING.en.md)
  — открытая точка входа для стороннего участника: где начать; четыре способа
  участвовать (issue; PR на документ/код; предложение в governance; путь к
  верифицированному участнику/хранителю); рельсы вклада (общественное благо НЕ
  инвестиция, TESTNET-first, секреты не в репо, двуязычность RU↔EN в одном PR,
  прозрачность через реестр, приватность заявителей); кодекс поведения; команды
  самопроверки (`registry.py verify`, `run_all.py --with-tests`); лицензии вклада.
  Ключевой конституционный акцент на видном месте — **вклад ≠ власть над казной**
  (распределением управляют конституция и голосование «1 человек = 1 голос»,
  авторство = признание, не собственность; согласовано с
  [`AUTHORS.md`](../AUTHORS.md) и [`GOVERNANCE.md`](GOVERNANCE.md)). Внесён в карту
  доков обоих README и в таблицу файлов корня REPO-STRUCTURE (RU/EN). Documentation-
  агент зелёный (пары/переключатель/ссылки), `run_all --with-tests`: агенты 8/8,
  тесты 8/8. `PTD-0031`. TESTNET-ONLY.
- **PTD-0030 (сессия 33):** Этап 6 (AI-агенты), стандарт качества — **тест-инвариант
  Audit**. [`test_audit.py`](../ai-agents/test_audit.py) (9/9) закрывает последний
  пробел каркаса 8/8: Audit был единственным агентом без собственного инварианта.
  Доказывает, что Audit **сводит вердикт честно, а не «зелёный по умолчанию»**:
  `run_check` (exit0→pass / exit1→fail / упавшая команда→error), сведение `main`
  (любой fail/error → red), и интеграция с НАСТОЯЩИМИ рельс-валидаторами
  (`safe_config.py`/`snapshot_config.py`) на отравленных ВРЕМЕННЫХ копиях конфигов
  (Safe с mainnet `chain_id`; Snapshot с плутократической стратегией) → красный, а
  на здоровом репо → зелёный. Реальные конфиги не изменяются. Добавлен в `run_all`
  (TESTS) → тесты 8/8. `PTD-0030`. **Все восемь агентов теперь покрыты инвариантами.**
  TESTNET-ONLY.
- **PTD-0029 (сессия 32):** Этап 6 (AI-агенты), консолидация каркаса 8/8 —
  **мета-агент Run-All**. [`run_all.py`](../ai-agents/run_all.py) — служебный
  read-only мета-модуль (ст. 9): запускает всех восьмерых агентов с `--json` и
  сводит их отчёты в один вердикт «зелёно/красно». Единая точка входа: локальный
  self-check одной командой вместо восьми; CI сжат с ~15 шагов до двух
  (`--with-tests` гоняет и агентов, и их семь тест-инвариантов); `--with-contracts`
  пробрасывается в Audit; `--json` — машиночитаемая сводка. Считает агента
  «красным» не только по его вердикту, но и при аномалии (невалидный JSON = агент
  упал; `verdict=green` при коде ≠ 0). **Тест-инвариант**
  [`test_run_all.py`](../ai-agents/test_run_all.py) (13/13) на фейковых агентах/
  тестах доказывает «красное сводится в красное, зелёное не ложно-падает». На
  реальном репо: агенты 8/8, тесты 7/7. `PTD-0029`. TESTNET-ONLY.
- **PTD-0027 (сессия 30):** Этап 6 (AI-агенты), модуль 7/8 — **Governance-агент**.
  [`governance_agent.py`](../ai-agents/governance_agent.py) — служебный read-only агент
  (ст. 9; **сам НЕ голосует**, ничего не вносит и ничем не распоряжается): превращает в
  машинную проверку жизненный цикл предложения из `GOVERNANCE.md` над конфигами управления
  `governance/snapshot/space.json` и `governance/safe/safe.config.json`. Шесть проверок:
  `one-person-one-vote` (стратегия голоса = `ticket` value=1, любая плутократия по балансу
  краснит — ст. 2/запрет №5), `timed-vote` (срок голосования `delay`/`period` > 0 — §7),
  `off-chain-signal` (`off_chain_signaling=true` и все типы `binding=false` — Snapshot
  обсуждает, исполняет Safe/Timelock — ст. 4/§5), `proposal-binding` (типы про деньги/
  конституцию привязаны к PRIORITIES/ANTI-ABUSE/CONSTITUTION + супербольшинство для поправок
  — ст. 5/§7–§8/ст. 10), `multisig-not-sole` (порог Safe ≥2 и < числа владельцев, 3-из-5,
  никто единолично — ст. 5/§5), `lifecycle-links` (все ссылки конфигов резолвятся — ст. 3).
  Тест-инвариант [`test_governance.py`](../ai-agents/test_governance.py) (26/26). CI
  `ai-agents.yml` расширен (+ тест Governance + Governance). На реальных конфигах: 6/6.
  `PTD-0027`. TESTNET-ONLY. Дальше — последний модуль 8/8 (Mediator).
- **PTD-0026 (сессия 29):** Этап 6 (AI-агенты), модуль 6/8 — **Documentation-агент**.
  [`documentation_agent.py`](../ai-agents/documentation_agent.py) — служебный
  read-only агент: превращает в машинную проверку правило проекта «вся документация
  двуязычна (RU↔EN)» и конституционную проверяемость (ст. 3) / открытость (ст. 6).
  Три проверки по git-отслеживаемым `.md`: `bilingual-pairs` (у каждого публичного
  дока есть пара RU↔EN — правило пар выводится из пути: `docs/NAME.md`↔`docs/en/NAME.md`;
  `<dir>/README.md`↔`<dir>/README.en.md`; корневой `NAME.md`↔`NAME.en.md`),
  `language-switcher` (вверху корректный переключатель `[Русский]·[English]` к
  парному файлу), `link-integrity` (все относительные ссылки резолвятся; внешние
  ссылки и блоки кода исключены — без ложных срабатываний). Служебные одноязычные
  файлы (`BUILDER`/`LAUNCH`/`PROGRESS`/`DECISIONS`/`comms`) исключены by design.
  Вывод человекочитаемый и `--json`; код 0/1; «красный» = сигнал. **Тест-инвариант**
  [`test_documentation.py`](../ai-agents/test_documentation.py) (17/17) доказывает
  «красное ловится, зелёное не ложно-падает». **Польза сразу:** при первом прогоне
  агент вскрыл реальный пробел двуязычности — у `governance/ipfs/README.md` и
  `governance/registry/README.md` не было EN-зеркал; добавлены EN-зеркала +
  переключатели (теперь 8/8 проверок зелёные). Закрывает и P2 «авто-проверка
  двуязычности в CI». CI [`ai-agents.yml`](../.github/workflows/ai-agents.yml)
  расширен (+ тест Documentation + Documentation; триггеры на `**/*.md`, `*.md`).
  `PTD-0026`. TESTNET-ONLY. Дальше — модули 7–8 (Governance / Mediator).
- **PTD-0025 (сессия 28):** Этап 6 (AI-агенты), модуль 5/8 — **Housing-агент**.
  [`housing_agent.py`](../ai-agents/housing_agent.py) — служебный read-only агент,
  профильный помощник по жилищным кейсам: доказывает, что модель целевого расхода
  ([`ESCROW-TARGETED-DISBURSEMENT.md`](ESCROW-TARGETED-DISBURSEMENT.md)) — «помощь
  оплачивается напрямую поставщику, не на руки» — заложена В КОД контракта
  [`Disbursement.sol`](../contracts/contracts/Disbursement.sol): `release-to-provider-only`
  (release без адреса-параметра → транш строго на c.provider), `provider-fixed`
  (нет setProvider/.provider=), `refund-to-treasury` (возврат в казну, не получателю),
  `tranche-limit` (потолок maxRelease), `guardian-cannot-move` (средства двигает
  только executor; guardian лишь ставит паузу) + проверки жилищных записей реестра
  (`targeted-escrow`/`provider-onchain`/`category-priority`, уровень читается из
  [`PRIORITIES.md`](PRIORITIES.md)). Вывод человекочитаемый и `--json`; код 0/1;
  «красный» = сигнал. **Тест-инвариант** [`test_housing.py`](../ai-agents/test_housing.py)
  (23/23) доказывает «красное ловится, зелёное не ложно-падает» (отравленные версии
  контракта/записей; не-жилищные записи игнорируются; отсев комментариев). CI
  [`ai-agents.yml`](../.github/workflows/ai-agents.yml) расширен (+ триггеры на
  contracts/contracts и docs/PRIORITIES.md). На реальном `Disbursement.sol`: 8/8.
  `PTD-0025`. TESTNET-ONLY. Дальше — модули 6–8 (Governance / Mediator / Documentation).
- **PTD-0024 (сессия 27):** Этап 6 (AI-агенты), модуль 4/8 — **Reputation-агент**.
  [`reputation_agent.py`](../ai-agents/reputation_agent.py) — служебный read-only
  агент: статически разбирает ончейн-контракт репутации
  [`Reputation.sol`](../contracts/contracts/Reputation.sol) и офчейн-настройки
  [`space.json`](../governance/snapshot/space.json) и доказывает, что модель
  «1 человек = 1 голос» ([`GOVERNANCE.md`](GOVERNANCE.md) §2–§3) сохранена В КОДЕ:
  `soulbound` (у бейджа нет функций перевода — голос не продать), `bounded-weight`
  (`votingUnits`: не участник → 0, участник → `1 + min(points, cap)`, коридор
  [1..1+cap] — власть денег невозможна), `no-funds` (слой репутации не двигает
  средства — «уникальность ≠ власть»), `roles-separated` (verifier подтверждает
  уникальность, governor правит параметры; роли не смешаны), `off-chain-equal`
  (стратегия Snapshot = равный `ticket` value=1, не плутократия по балансу; допуск
  только участникам). Вывод человекочитаемый и `--json`; код 0/1; «красный» =
  сигнал. **Тест-инвариант** [`test_reputation.py`](../ai-agents/test_reputation.py)
  (17/17) доказывает «красное ловится, зелёное не ложно-падает» (включая отсев
  упоминаний в комментариях Solidity). CI
  [`ai-agents.yml`](../.github/workflows/ai-agents.yml) расширен. `PTD-0024`.
  TESTNET-ONLY. Дальше — модули 5–8 (Housing / Governance / Mediator / Documentation).
- **PTD-0023 (сессия 26):** Этап 6 (AI-агенты), модуль 3/8 — **Fairness-агент**.
  [`fairness_agent.py`](../ai-agents/fairness_agent.py) — служебный read-only агент:
  проходит по записям публичного реестра типа `disbursement` и проверяет КАЖДУЮ
  выплату на справедливость распределения и антизлоупотребление — `priority-valid`
  (уровень приоритета в шкале 1..10, читается ПРЯМО из [`PRIORITIES.md`](PRIORITIES.md)),
  `safeguards` (приоритет не отключает `limit_ok`/`collective_review`/окно апелляции),
  `collective-review` (≥2 независимых подтверждения — не единолично), `staged-payments`
  (поэтапность `1<=index<=of`), `applicant-privacy` (нет персональных данных — только
  псевдонимный `case_id`). Вывод человекочитаемый и `--json`; код 0/1; «красный» =
  сигнал. **Тест-инвариант** [`test_fairness.py`](../ai-agents/test_fairness.py) (17/17)
  доказывает «красное ловится, зелёное не ложно-падает» (включая пустой реестр).
  **Побочно:** найден и исправлен латентный баг с сессии 25 — Guardian ложно краснел
  на собственном тест-инварианте (строка-фикстура `private_key: 0x...`), из-за чего
  CI `ai-agents` на `main` был фактически красным; Guardian научен пропускать
  текстовый скан тест-инвариантов агентов (`ai-agents/test_*.py`), +регрессионный
  сценарий. CI [`ai-agents.yml`](../.github/workflows/ai-agents.yml) расширен. `PTD-0023`.
  TESTNET-ONLY. Дальше — модули 4–8 (Reputation / Housing / …).
- **PTD-0022 (сессия 25):** Этап 6 (AI-агенты), модуль 2/8 — **Guardian-агент**.
  [`guardian_agent.py`](../ai-agents/guardian_agent.py) — отдельный явный сканер рельс
  безопасности по ВСЕМУ дереву репо (по git-отслеживаемым файлам): `secrets-tracked`
  (нет закоммиченных секретов/ключей/состояния пульса), `gitignore-guards`
  (`.gitignore` прикрывает `.env`/`logs/`), `no-mainnet` (нет mainnet `chain_id` в JSON),
  `no-key-material` (нет приватных ключей в тексте: 64-hex вне хэш-полей + присваивание
  `private key`/`mnemonic`/`seed`). Без ложных срабатываний на легитимных sha256/CID
  реестра и манифеста (64-hex считается ключом только вне хэш-полей). **Тест-инвариант**
  [`test_guardian.py`](../ai-agents/test_guardian.py) (14/14) на временном git-репо
  доказывает «красное реально ловится, зелёное не ложно-падает». CI
  [`ai-agents.yml`](../.github/workflows/ai-agents.yml) расширен (Audit + тест Guardian +
  Guardian). `PTD-0022`. TESTNET-ONLY. Дальше — модули 3–8 (Fairness / Reputation / …).
- **PTD-0021 (сессия 24):** Этап 6 (AI-агенты), модуль 1/8 — **каркас
  [`ai-agents/`](../ai-agents/) + первый рабочий агент Audit**. README (RU/EN)
  фиксирует принцип ст. 9 «ИИ служит, не правит» и границы для всех агентов
  (read-only к средствам, находка = сигнал, не действие), таблицу восьми агентов.
  [`audit_agent.py`](../ai-agents/audit_agent.py) объединяет четыре рельс-валидатора
  (`registry`/`ipfs`/`safe`/`snapshot`) в один аудит-прогон и связывает каждую
  проверку со статьёй конституции (ст. 3/2/4); опция `--with-contracts` добавляет
  тесты контрактов (ст. 4/7); вывод человекочитаемый и `--json`; код возврата 0/1.
  CI [`ai-agents.yml`](../.github/workflows/ai-agents.yml) на push/PR. «До зелёного»:
  4/4 базовый, 5/5 с контрактами (54 теста). Реализует и идею «единый governance-
  валидатор в CI». При первом прогоне агент поймал реальный рассинхрон IPFS-манифеста
  (исправлен). TESTNET-ONLY. Дальше — модуль 2/8 (Guardian) и остальные.
- **PTD-0020 (сессия 23):** Этап 5 (Смарт-контракты), часть 3c — **сборка всего
  он-чейн контура**. [`contracts/scripts/deploy.js`](../contracts/scripts/deploy.js)
  разворачивает пять контрактов и связывает их в единый механизм
  (Reputation→Timelock→Treasury/Disbursement→Governor): `executor` казны/escrow =
  `Timelock`, `governor` Timelock = `Governor`, bootstrap-admin снят (`renounceAdmin`),
  `Reputation.governor` = `Timelock`. После проводки никто не двигает средства
  единолично (деплойер без привилегий, исполняет только прошедшее+отложенное
  голосование). Интеграционный тест [`Integration.test.js`](../contracts/test/Integration.test.js)
  прогоняет главный сценарий фонда «заявка-кейс → прямое голосование → задержка
  `Timelock` → **целевая выплата напрямую поставщику** через `Disbursement`», а также
  guardian-вето и проверку проводки; +4 теста «до зелёного» (54/54 со всеми
  контрактами). `npm run deploy:local` для демо. TESTNET-ONLY. Этап 5 (каркас)
  собран целиком; дальше — часть 4 (публичный testnet, сеть с оператором) либо Этап 6.
- **PTD-0019 (сессия 22):** Этап 5 (Смарт-контракты), часть 3b — контракты
  [`Governor.sol`](../contracts/contracts/Governor.sol) + [`Timelock.sol`](../contracts/contracts/Timelock.sol)
  по [`GOVERNANCE.md`](GOVERNANCE.md) §4–§7. Прямое голосование верифицированных
  участников: `propose`/`castVote`/`queue`/`execute`, вес голоса из
  `Reputation.votingUnits` («1 человек = 1 голос», не плутократия), кворум/срок,
  публичный детерминированный подсчёт. Принятое решение исполняется **только через
  `Timelock`** (обязательная задержка = окно на аудит/апелляцию; Governor сам
  средства не двигает — двигает казна по приказу Timelock). `guardian` = аварийное
  вето (`cancel`), `admin` = разовый bootstrap (`renounceAdmin`); параметры и роли
  меняются только голосованием (`onlyTimelock`/`onlySelf`). 15 тестов «до зелёного»,
  включая полный цикл «предложение → голос → Timelock → выплата поставщику» (50/50
  со всеми контрактами). TESTNET-ONLY. Дальше — часть 3c (сборка контура) и часть 4
  (testnet-деплой).
- **PTD-0018 (сессия 21):** Этап 5 (Смарт-контракты), часть 3a — контракт
  [`Reputation.sol`](../contracts/contracts/Reputation.sol): непередаваемый
  (soulbound) бейдж верифицированного участника по [`GOVERNANCE.md`](GOVERNANCE.md)
  §2–§3. «1 человек = 1 голос» в коде: `votingUnits(addr)` = 0 для не-участника и
  1 + min(`reputationPoints`, `reputationCap`) для участника — вес всегда в коридоре
  [1..1+cap], власть денег невозможна. Soulbound: у контракта **нет функций перевода**
  (transfer/approve/transferFrom) by design. «Уникальность ≠ власть»: `verifier`
  выдаёт/отзывает бейдж, `governor` правит параметры, ни одна роль не двигает
  средства. Отзыв сбрасывает вес в 0 и репутацию. 11 тестов «до зелёного» (35/35 с
  Treasury+Disbursement). TESTNET-ONLY. Дальше — часть 3b (`Governor`+`Timelock`).
- **PTD-0017 (сессия 20):** Этап 5 (Смарт-контракты), часть 2 — контракт целевого
  escrow [`Disbursement.sol`](../contracts/contracts/Disbursement.sol) по
  [`ESCROW-TARGETED-DISBURSEMENT.md`](ESCROW-TARGETED-DISBURSEMENT.md): `open` фиксирует
  поставщика в кейсе, `release(id, amount)` **не принимает адрес получателя** — транш
  уходит строго поставщику («не даём деньги — оплачиваем нужду»); `refund` возвращает
  остаток в казну (не получателю); поэтапность (накопление `released` + лимит транша
  `maxRelease`); обеспеченность escrow (`available()`); только `executor` двигает,
  `guardian` только пауза; события + `registryRef`. 14 тестов «до зелёного» (24/24 с
  Treasury). Реестровая схема `disbursement` расширена `provider/category/escrow_id`.
  TESTNET-ONLY. Дальше — часть 3 (`Governance` + `Reputation`).
- **PTD-0016 (сессия 19):** Этап 5 (Смарт-контракты), часть 1 — проект
  [`contracts/`](../contracts/) (Hardhat + ethers v6 + chai, Solidity 0.8.24) +
  базовый контракт-скелет [`Treasury.sol`](../contracts/contracts/Treasury.sol):
  казна отдаёт средства только через `executor` (мультисиг/Timelock), лимит одной
  выплаты, аварийная пауза `guardian`, события на каждое движение + `registryRef`
  (связь с реестром). 10 тестов конституционных свойств «до зелёного» + CI
  [`contracts.yml`](../.github/workflows/contracts.yml). TESTNET-ONLY, без реальных
  средств/ключей. Открывает Этап 5 (части 2–4 — Disbursement/Governance/Reputation/деплой).
- **PTD-0015 (сессия 18):** макет off-chain голосования
  [`governance/snapshot/`](../governance/snapshot/) — `space.json` (настройки
  Snapshot: стратегия `ticket` value=1 = «1 человек = 1 голос», допуск только
  верифицированных, типы предложений, привязка к GOVERNANCE/PRIORITIES/CONSTITUTION)
  + JSON-схема + README (RU/EN) + валидатор рельс `scripts/snapshot_config.py` (только
  testnet, без плутократии по балансу токена, анти-Сивилла, без приватных ключей) + CI.
  Snapshot = палата обсуждения/сигнала, средства не двигает. Закрывает Этап 4 часть 1.
- **PTD-0014 (сессия 17):** модель поддержки [`SUPPORT-MODEL.md`](SUPPORT-MODEL.md)
  (+EN) — поддержка приходит из самой работающей системы (прозрачная казна-мультисиг
  3-из-5 + контракты, каждый поток в реестре и on-chain, расход по голосованию), а НЕ
  отдельной кнопкой/адресом сбоку. До запуска и аудита адрес для реальных денег не
  публикуется. На сайте — честное объяснение без кнопки-доната. Закрывает INBOX-пункт о донатах.
- **PTD-0012 (сессия 15):** аутрич [`OUTREACH.md`](OUTREACH.md) (+EN) —
  категории адресатов (с приоритетами P0–P3) + где искать официальный публичный
  контакт, правила персонализации и «лестница просьб», 5 шаблонов писем RU + 5 EN,
  регламент после ответа, таблица «агент vs оператор». Заготовка «до кнопки»:
  отправляет оператор лично, агент не рассылает. Закрывает INBOX #8.
- **PTD-0011 (сессия 14):** почта проекта [`EMAIL-SETUP.md`](EMAIL-SETUP.md)
  (+EN) — сравнение вариантов (свой домен / ProtonMail / Gmail) с рекомендацией,
  адреса/алиасы, пошаговая инструкция оператору (Proton-старт + свой домен с
  MX/SPF/DKIM/DMARC), подпись-boilerplate, шаблоны ответов RU/EN. Заготовка «до
  кнопки»: регистрацию ящика и хранение пароля делает оператор. Закрывает INBOX #7.
- **PTD-0006 (сессия 9):** материалы продвижения [`PROMOTION.md`](PROMOTION.md)
  (+EN) — рельсы сообщения, boilerplate+дисклеймер, лендинг-копия, питч RU/EN
  (3 формата), пост-анонс (Telegram + Twitter/X), FAQ «общественное благо, НЕ
  инвестиция», чек-лист публикации. Все тексты сверены с конституционными
  запретами. Заготовки «до кнопки» — публикует оператор. Закрывает INBOX #6.
- **PTD-0005 (сессия 8):** заведён сам механизм саморазвития — этот `ROADMAP.md`
  (+EN), правило в `BUILDER.md` («INBOX пуст → следующий пункт ROADMAP») и режим
  автономии по умолчанию (INBOX #9). Закрывает INBOX #5 и #9.

---

## Лог идей (кто/когда предложил)

Чтобы саморазвитие было прозрачным, фиксируем происхождение идей.

| Идея | Источник | Сессия |
|------|----------|--------|
| Механизм саморазвития (ROADMAP + правило) | оператор (INBOX #5) | 8 |
| Режим автономии по умолчанию | оператор (INBOX #9) | 8 |
| CONTRIBUTING / глоссарий / страница прозрачности / CI-двуязычность | агент | 8 |
| Дашборд казны / шаблоны заявок / changelog / репутация | агент | 8 |
| Лендинг-страница в web / медиа-кит / пресс-страница | агент | 9 |
| Шаблоны предложений Snapshot / единый governance-валидатор в CI | агент | 18 |
| Тест-инвариант Audit / Guardian-агент / Documentation-агент (двуязычность) | агент | 24 |
| Мета-агент «прогнать всех» / pre-commit-хук / лексический линтер запретов | агент | 25 |
| Тест-инвариант «выплата ↔ запись реестра» / самопроверка агентов в CI / Reputation-агент | агент | 26 |
| Reputation на Governor.sol / проверка cap деплоя / общий solidity_scan.py | агент | 27 |
| Рефакторинг общих solidity-помощников (3 копии) / Housing provider-whitelist / сквозной тест «запись ↔ on-chain escrow» | агент | 28 |
| Мета-агент run_all / линтер запретов в публичных текстах / changelog из реестра | агент | 29 |
| Статус-бейдж run_all / тест-инвариант Audit / общий agent_report.py | агент | 32 |
| Чистка диска (node_modules) / стандарт «все агенты с инвариантами» / sol-helpers-рефактор | агент | 33 |
| CONTRIBUTING (выполнен) / CODE_OF_CONDUCT отдельным файлом / шаблоны issue+PR | агент | 34 |
| Глоссарий (выполнен) / линк на глоссарий в шапке доков / агент-проверка терминов | агент | 36 |
| Шаблоны issue/PR (выполнено) / GitHub-метки в .github/labels / автонастройка веток | агент | 37 |
| Страж: CI зовёт run_all (выполнено) / страж проверяет триггер-пути воркфлоу (on.push.paths включает ai-agents/**) / Documentation-агент: мягкая проверка «указатель в шапке ведёт в глоссарий» | агент | 60 |
| Страж: триггер-пути включают ai-agents/** (выполнено) / страж проверяет, что воркфлоу зовёт и test_run_all.py | агент | 63 |
| Машиночитаемый статус-светофор run_all (выполнено) / публичный статус-светофор на сайте из артефакта / страж: артефакт статуса не отстал от вердикта | агент | 72 |
| Публичный светофор на сайте (выполнено) / компактный значок-светофор на остальных страницах / значок также в шапке (topbar), не только в футере | агент | 73 |
| Каталог меток .github/labels.yml (выполнено) / опциональный синхронизатор меток / страж: каталог меток без дублей и с валидным hex-цветом | агент | 76 |
| Экран «Что приложение помнит о вас» (выполнено) / кнопка «стереть мои данные» прямо на экранах «Голосование» и «Проверка личности» со ссылкой на «Мои данные» / экспорт всех черновиков одним файлом (бэкап для человека) | агент | 88 |
| «Забрать копию» данных одним файлом (выполнено) / обновить Next с 15.1.6 на пропатченную версию (security-advisory CVE-2025-66478) / кнопка «забрать копию» также видна, когда список пуст (честный пустой файл-шаблон) | агент | 90 |
| Экран «Порядок помощи» (выполнено) / Documentation-агент: мягкая проверка «экран-зеркало в platform/app не отстал от нормативного дока» (priorities↔PRIORITIES, glossary↔GLOSSARY) / экран «Окно казны» связать ссылкой с «Порядком помощи» (пулы помощи ↔ кому раньше) | агент | 91 |
| Экран «Помощь и благодарность за труд» (выполнено) / экран «Окно казны» связать ссылкой с «Помощь и награда» (пул помощи / пул благодарности ↔ как делятся деньги) / экран «Подать заявку» связать с «Помощь и награда» (откуда берётся оплата нужды) / экран-зеркало PROOF-OF-CONTRIBUTION «Как фонд создаёт честную работу» | агент | 93 |
| Линтер конституционных запретов в публичных текстах (выполнено) / расширить линтер на тексты внутри `platform/lib/i18n.ts` (видимая копия приложения, не только .md/web) / линтер «обещание без оговорки» — публичный текст рядом с суммой/процентом обязан иметь дисклеймер «это не доход» | агент | 98 |
